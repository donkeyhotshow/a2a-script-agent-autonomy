---
description: Documentation hierarchy and organization rules
globs: **/*.md
alwaysApply: true
---

# Document Hierarchy Rules

## Required Files by Level

### Root Level (repo-root/)
```
README.md          # Project overview
AGENTS.md          # Agent guidance (always-applied rules)
DEV_STATE.md       # Cross-module state
docs/adr/          # Architecture decisions
tasks/             # Pending work queue
```

### Module Level (a2a-*/)
```
README.md          # Module purpose + quickstart
DEV_STATE.md       # Module-specific implementation state
```

### Feature Level (*/feature/)
```
README.md          # Feature docs
```

## DEV_STATE Protocol

**Always update before/after tasks:**

1. **Prune** — Remove completed items, duplicates
2. **Discover** — Scan for new work from code/sims/risks
3. **Write** — Add concrete testable tasks

### Content Rules

- Root: Cross-module facts only
- Each module: Own implementation details
- No abstract statements — all tasks testable
- Tasks >14 days → backlog with blocker reason
- **Mandatory:** root `DEV_STATE.md`, every `a2a-*/DEV_STATE.md`, and `AGENTS.md` Quick Reference must link [`docs/PROJECT-RULES-QA.md`](docs/PROJECT-RULES-QA.md) (project rules yes/no log)

## Empty Queue Protocol

Empty backlog = **NOT** finished. Required actions:

1. Prune root + module DEV_STATE.md
2. Discover work (code, sims, risks, backlog)
3. Write tasks to DEV_STATE + tasks/pending/
4. Resume normal cycle

## Quick Reference Table

| Document | Purpose | Update When |
|----------|---------|-------------|
| AGENTS.md | Agent rules | Architecture changes |
| docs/PROJECT-RULES-QA.md | Rules interview log (yes/no) | New answered rule |
| DEV_STATE.md | Current state | Before/after tasks |
| docs/adr/*.md | Decisions | New patterns |
| docs/TRIANGLE-WORKFLOW.md | Stack triage (A/B/C), loop 0–4 | Session/async/debug workflow changes |
| GLOSSARY.md | Terminology + **Alerts** vs **Rooms** | New concepts or triage labels |
| tasks/pending/*.md | Work queue | New tasks found |

## README Requirements

Every README must have:
- Purpose statement (1 sentence)
- Quickstart or link to one
- Key files/patterns section

## AI-Only Documentation Style (Mandatory)

Assume docs and inline comments are interpreted by AI agents.

Required writing policy:

- Maximum semantic unambiguity; avoid metaphor and narrative filler.
- State assumptions and constraints explicitly.
- For procedures/behaviors, define inputs, outputs, and side effects.
- If text admits multiple interpretations, enumerate interpretations, select one, and explain selection reason.
- Prefer deterministic language (`must`, `must not`, `if/then`) over human-oriented prose.

Required sections for new/updated procedural docs:

1. Inputs
2. Outputs
3. Side effects
4. Assumptions
5. Constraints
6. Ambiguities and chosen interpretation
