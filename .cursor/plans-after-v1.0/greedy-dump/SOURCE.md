# Source of truth (external)

**Path:** `C:\workspace\domain-platform\markdown-pipeline-automator\work`

| Item | Note |
|------|------|
| `LEGACY-CODEBACE.zip` | ~466 MB — **do not** vendor into git; reference or unpack outside the repo if needed. |
| `enggineered-prompts/` | Prompts, Cursor story assets, zips, mixed encodings. |
| `obrabotano/` | Post-processed / terminal-related material (`node-terminal`, …). |
| `priority-1` … `priority-6` | Priority buckets; `priority-4` is empty; `priority-3` and `priority-6` are very large file counts. |

Mirroring policy: copy **small** text/config samples into `greedy-dump/mirror/.../stubs/` by hand or script when a task needs them; bulk binary trees stay at SOURCE.
