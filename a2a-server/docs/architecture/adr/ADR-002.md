# ADR-002: Протокол коммуникации A2A (JSON-RPC 2.0)

## Дата
2026-02-25

## Авторы
- Команда разработки

## Статус
Предложено

## Контекст

Система A2A Script Agent использует JSON-RPC 2.0 протокол для коммуникации между клиентом и сервером. Протокол определяет формат запросов, ответов и механизм генерации идентификаторов для отслеживания запросов.

### Требования к протоколу

- **Версия JSON-RPC:** 2.0
- **Транспорт:** HTTP и WebSocket
- **Кодировка:** UTF-8
- **Типы запросов:** JSON object (не массивы)

## Решение

### Формат запроса (Request)

```typescript
interface JSONRPCRequest {
  jsonrpc: "2.0";           // Версия протокола
  id: string;               // Уникальный идентификатор запроса
  method: string;           // Имя метода
  params?: object;          // Параметры метода (опционально)
}
```

### Формат ответа (Response)

```typescript
interface JSONRPCResponse {
  jsonrpc: "2.0";           // Версия протокола
  id: string;              // Идентификатор запроса
  result?: object;         // Результат (при успехе)
  error?: JSONRPCError;    // Ошибка (при неудаче)
}

interface JSONRPCError {
  code: number;             // Код ошибки
  message: string;         // Сообщение об ошибке
  data?: object;           // Дополнительные данные
}
```

### Коды ошибок JSON-RPC

| Код | Название | Описание |
|-----|----------|----------|
| -32700 | Parse error | Неверный JSON |
| -32600 | Invalid Request | Неверный формат запроса |
| -32601 | Method not found | Метод не найден |
| -32602 | Invalid params | Неверные параметры |
| -32603 | Internal error | Внутренняя ошибка сервера |
| -32000 | Server error | Ошибка на стороне сервера |
| -32001 | Unauthorized | Не авторизован |
| -32002 | Timeout | Таймаут выполнения |

### Структура Message Flow

```
Client                                          Server
  |                                               |
  |  POST /api/v1/requests                       |
  |  {                                           |
  |    "jsonrpc": "2.0",                         |
  |    "id": "req_xxxxx",                        |
  |    "method": "tasks/send",                  |
  |    "params": {                               |
  |      "task": { ... },                       |
  |      "context": { ... }                     |
  |    }                                         |
  |  }                                           |
  |-------------------------------------------->|
  |                                               |
  |  Response: 202 Accepted                      |
  |  {                                           |
  |    "jsonrpc": "2.0",                         |
  |    "id": "req_xxxxx",                        |
  |    "result": {                               |
  |      "promiseId": "promise_xxxxx",          |
  |      "status": "pending"                     |
  |    }                                         |
  |  }                                           |
  |<--------------------------------------------|
  |                                               |
  |  GET /api/v1/requests/:promiseId/status      |
  |-------------------------------------------->|
  |                                               |
  |  {                                           |
  |    "jsonrpc": "2.0",                         |
  |    "id": "req_xxxxx",                        |
  |    "result": {                               |
  |      "promiseId": "promise_xxxxx",          |
  |      "status": "processing"                  |
  |    }                                         |
  |  }                                           |
  |<--------------------------------------------|
  |                                               |
  |  ... polling ...                             |
  |                                               |
  |  GET /api/v1/requests/:promiseId/result      |
  |-------------------------------------------->|
  |                                               |
  |  {                                           |
  |    "jsonrpc": "2.0",                         |
  |    "id": "req_xxxxx",                        |
  |    "result": {                               |
  |      "promiseId": "promise_xxxxx",          |
  |      "status": "completed",                  |
  |      "data": { ... }                         |
  |    }                                         |
  |  }                                           |
  |<--------------------------------------------|
```

### Генерация ID

Используется формат: `req_<timestamp>_<random>` или `uuid v4`

```typescript
function generateRequestId(): string {
  const timestamp = Date.now().toString(36);
  const random = Math.random().toString(36).substring(2, 15);
  return `req_${timestamp}_${random}`;
}

function generatePromiseId(): string {
  const timestamp = Date.now().toString(36);
  const random = Math.random().toString(36).substring(2, 15);
  return `promise_${timestamp}_${random}`;
}
```

### Методы API

| Метод | Описание | Параметры |
|-------|----------|-----------|
| `tasks/send` | Отправить задачу | task, context |
| `tasks/get` | Получить задачу | promiseId |
| `tasks/cancel` | Отменить задачу | promiseId |
| `tasks/subscribe` | Подписаться на обновления | promiseId |
| `session/create` | Создать сессию | sessionData |
| `session/update` | Обновить сессию | sessionId, updates |
| `auth/token` | Получить токен | credentials |

## Последствия

### Положительные

- **Стандартизация:** JSON-RPC 2.0 — широко известный стандарт
- **Простота:** Легкая реализация на любом языке
- **Идемпотентность:** Возможность повторных запросов
- **Асинхронность:** Поддержка long-running задач

### Отрицательные

- **Опрос (polling):** Требует периодического опроса статуса
- **Размер payload:** JSON может быть избыточным для больших данных

### Ограничения

- Максимальный размер payload: 10MB
- Таймаут ожидания результата: 300 секунд (настраивается)
- Rate limiting: 100 запросов в минуту на клиента

## Связанные ADR

- [ADR-001: Архитектура A2A Script Agent](./ADR-001.md)
- [ADR-003: State Machine (PhaseMachine)](./ADR-003.md)
- [ADR-006: WebSocket vs HTTP](./ADR-006.md)

## История изменений

| Дата | Версия | Описание |
|------|--------|----------|
| 2026-02-25 | 1.0 | Начальная версия ADR |
