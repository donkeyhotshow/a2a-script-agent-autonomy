# Server interrupt loop (renamed)

This document was **renamed** to match the **gray room** vocabulary used in [WORKFLOW.md](../../docs/WORKFLOW.md).

**Canonical spec:** [**Gray room**](./GRAY-ROOM.md) (`a2a-server/docs/GRAY-ROOM.md`)

The old title **“server-side interrupt loop”** is still accurate in code (`processDialogResponseWithInterruptLoop`, env `A2A_MAX_INTERRUPT_TURNS`). All technical detail lives under **Gray room**.
