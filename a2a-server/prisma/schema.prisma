// Prisma Schema for A2A Server
// Simplified: No Project/Session storage - only accepts context from client

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["fullTextSearch", "fullTextIndex"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
