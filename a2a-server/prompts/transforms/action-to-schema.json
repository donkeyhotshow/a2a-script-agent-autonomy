{
  "description": "Transform schema mapping for action-to-schema resolution",
  "mapping": {
    "dialog": "dialog",
    "agent": "agent",
    "task-decomposition": "task-decomposition"
  },
  "notes": {
    "dialog": "Dialog pipeline: user message → LLM → response with message/form",
    "agent": "Agent pipeline: tools + message → LLM → response with execute (tools)",
    "task-decomposition": "Task decomposition pipeline: analyze task → break into subtasks"
  }
}