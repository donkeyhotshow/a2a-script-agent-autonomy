# AI Session Context System (AI Prompt Saver)

> **Относится к:** a2a-server (actions) - НЕ a2a-client

## ВАЖНО: Архитектура

- **Сервер НЕ хранит данные** - согласно AGENTS.md "Server does NOT store client data"
- **Сервер создаёт файлы** - это приложение агента, сервер создаёт документы контекста
- **Клиент передаёт историю** - контекст из предыдущих сессий передаётся от клиента в incoming context
- **Документы возвращаются клиенту** - сервер не персистит, только обрабатывает и возвращает

> **Примечание:** Это последовательность для action на сервере (в отличие от первоначального описания где ошибочно указан a2a-client)

## Текущее состояние

### Реализация
Action definition уже создан: [`a2a-server/src/actions/definitions/ai-session-context.md`](a2a-server/src/actions/definitions/ai-session-context.md)

### Описание системы
Система для сохранения и использования контекста из предыдущих сессий при работе с новыми задачами.

### Рабочий процесс (существующий)
1. **Создание сессии**: При новой сессии создается отдельный JSON документ (конвертируется в MD)
2. **Добавление запроса**: То что пользователь написал в новый чат добавляется в конец файла сессии
3. **Поиск по индексу**: Получаем массив релевантных частей файлов и пути к файлам
4. **Уточнение задачи LLM**: Контекст (вектор данных + задача) → LLM → уточнения записываются в файл
5. **План изучения**: Повторный поиск → LLM → план изучения задачи → записывается в конец файла
6. **План правок**: Сбор полного контекста + граф → LLM → план правок по кодбейсу
7. **Выполнение**: Агент работает в YOLO режиме
8. **Контекстное подкидывание**: Система может изредка подкидывать контекст или корректировать

## План улучшений

> **Примечание:** Все эти функции реализованы в action definition. Сервер не хранит данные - только создаёт и возвращает документы клиенту.

### 1. Формат хранения сессий (в документе)
- [x] Структура документа (заголовок, метаданные, секции)
- [x] Версионирование сессий
- [x] Метаданные сессии (дата, проект, статус)
- [x] Приоритет: реализовано в action

### 2. Интеллектуальный поиск контекста
- [x] Weighted search - разные типы контекста имеют разный вес
- [x] Гибридный поиск (BM25 + semantic)
- [x] Фильтрация по типу файлов (только релевантные расширения)
- [x] Max tokens limit для контекста
- [x] Приоритет: реализовано

### 3. Улучшение pipeline уточнений
- [x] Multi-step уточнения (несколько итераций)
- [x] Feedback loop - уточнения влияют на следующий поиск
- [x] Context-aware уточнения (учитывают предыдущие сессии)
- [x] Приоритет: реализовано

### 4. Улучшение планирования
- [x] Структурированный план изучения (файлы, области кода, зависимости)
- [x] Приоритизация задач в плане
- [x] Оценка сложности задач
- [x] Dependencies между подзадачами
- [x] Приоритет: реализовано

### 5. YOLO Mode улучшения
- [x] Safety checks перед выполнением
- [x] Rollback механизм
- [x] Parallel execution с зависимостями
- [x] Progress tracking
- [x] Приоритет: реализовано (отдельный action)

### 6. Контекстное подкидывание
- [x] Triggers для подкидывания контекста
- [x] Priority queue для контекстов
- [x] Context expiration (старый контекст не актуален)
- [x] Context weighting по relevance
- [x] Приоритет: реализовано

### 7. Интеграция с сервисами
- [x] RAG Index - поиск релевантного кода
- [x] FileScanner - сканирование файлов
- [x] Embedding Service - семантический поиск
- [x] Context передаётся от клиента (не хранится на сервере)
- [x] Приоритет: реализовано в action

### 8. Actions (выполнение на сервере)
- [x] Action execution engine - выполняет команды из ответов сервера
- [x] Command parser - парсит команды от сервера
- [x] Context return - возвращает контекст на сервер без изменений
- [x] AI Session Context action - создаёт план изучения за первую итерацию
- [x] Приоритет: реализовано

### 8. UI/UX
- [ ] Визуализация плана сессии
- [ ] Редактирование плана вручную
- [ ] История сессий
- [ ] Экспорт/импорт сессий
- [ ] Приоритет: низкий

## Критерии приёмки

1. Система работает с существующим RAG индексом
2. Контекстные уточнения улучшают качество плана
3. YOLO mode безопасен (есть rollback)
4. Подкидывание контекста работает корректно
5. Обратная совместимость с текущим API
