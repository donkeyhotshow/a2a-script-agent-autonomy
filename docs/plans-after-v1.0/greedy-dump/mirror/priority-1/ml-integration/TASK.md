# Subtask: ml-integration

**Contents:** ML integration helpers (likely Python/Node bridges).

**Integration:** Isolate external calls; map to actions with explicit timeouts; align model endpoints with ai-integration/Ollama ports.
