# Task: priority-2 → script-agent

**Why:** Contains **a2a**-named material—should align or merge with this repo’s server actions.

**Order:** `a2a` first, then `agent.openrouter.ai`, then `laravel-agent-workspace-tools`.
