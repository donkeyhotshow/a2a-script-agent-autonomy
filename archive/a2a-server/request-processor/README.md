# Archived request-processor snippets

**`decision-cell.ARCHIVED.md`** — last TypeScript snapshot of the retired standalone LLM decision cell (ADR-0088). Gray Room gates **SIEGE_REVIEW** on the primary turn’s **`result.completed`** from response transforms (`agent-response.json`, `dialog-response.json`, etc.), not a second hub call.
