# Greedy dump: integrate scripts into a2a-server

**Source:** `C:\workspace\domain-platform\markdown-pipeline-automator\work` (originals stay there).  
**Queue:** `greedy-dump/` — see `TASK-TREE.md`, `STATE.md`, per-folder `TASK.md`.

**Definition of done (incremental):**

1. Pick next unchecked leaf in `greedy-dump/TASK-TREE.md`.
2. Implement or explicitly defer with reason in `greedy-dump/docs/DOCUMENTS-STATE.md`.
3. Server changes follow action-key shape; add sim when behavior is stable.
4. Update `greedy-dump/STATE.md` sequence.

**Priority hint:** `priority-1` → `priority-2/a2a` before wide `priority-3` scraping.
