# S8: Step-contract — 47 предупреждений только в simulations/sync

## Проблема
- При запуске `sim:validate --all --step-contract` в `simulations/sync` генерируется 47 предупреждений
- Предупреждения указывают на нарушение контракта формы

## Детали
- Все предупреждения в директории `simulations/sync`
- Типичные нарушения: отсутствие `title`, `description`, неправильная структура `choices`

## Критерий 完成
- Ноль предупреждений step-contract в sync-симуляциях
- Все формы валидны по контракту