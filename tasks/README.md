# tasks/

Task specs for sync sims, parity, and system backlog: `script-dialog-agent-response-parity.md`, `sync-*.md`, `system-improvement-priorities.md`.

Operator flow (human / Cursor, **curl**, system prompts): **`docs/OPERATOR-CURL.md`**.
