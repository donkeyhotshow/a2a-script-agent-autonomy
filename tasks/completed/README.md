# Completed task archive

Completed task write-ups are folded into `docs/` (see root `DEV_STATE.md` *Recent (docs)* for pointers). This folder stays for optional short-lived notes; do not revive long-lived duplicates of `docs/`.
