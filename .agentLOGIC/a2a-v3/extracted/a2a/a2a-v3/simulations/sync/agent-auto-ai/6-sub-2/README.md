# Step 6 — sub 2: `compress_history` then return

Sister folder next to [`../6/`](../6/). **Server-internal** snapshot; sidecar compress LLM, same `execute.write-file` as primary turn, no second main LLM.
