# Run hierarchical stack tests (Level 1–3)

## Sources

- [`scripts/tests/README.md`](../../scripts/tests/README.md)
- [`scripts/tests/run-all.ps1`](../../scripts/tests/run-all.ps1)

## Agent prompt (copy)

With the live stack started per `AGENTS.md`, run `.\scripts\tests\run-all.ps1` from repo root on Windows (or document Linux equivalent). Fix failures or file issues under `tasks/` + `DEV_STATE.md`. Use `-ContinueOnError` only for diagnosis.

## Completion

- [ ] Done (green run or tracked failures)
