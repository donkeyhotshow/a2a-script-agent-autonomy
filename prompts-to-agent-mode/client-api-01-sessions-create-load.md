# Client API: session create / load

## Sources

- [`a2a-client/docs/api-testing-plan.md`](../a2a-client/docs/api-testing-plan.md) — §1 Create Session + **Minimal Acceptance Checklist** first item
- [`AGENTS.md`](../AGENTS.md) — Unified manual path

## Agent prompt (copy)

With stack up (`start-all`), verify `POST /api/a2a/sessions` returns `session.id` and step `1/` has `server-response.json` and `messages.json`. Record evidence path or failure in checklist / `DEV_STATE.md`.

## Completion

[X] Completed
