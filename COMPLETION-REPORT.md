# Task Monitor — completion / history

**Operator doc:** [`MONITOR-QUICK-START.md`](MONITOR-QUICK-START.md) (commands, env, promise queue, tests).

**Implementation map:** same file → *Implementation map* table (entry + `tests/monitor-tasks/*.js`).

**Regression tests:** `npm run test:monitor` (19 static tests; repo root).

**Change archaeology:** use `git log -- monitor-and-process-tasks.js tests/monitor-tasks tests/infrastructure/monitor-and-process-tasks.test.js`.

This file is a **stub index** so prompts that reference `COMPLETION-REPORT.md` have a stable anchor; expand with dated narratives if you need a long-form incident log.
