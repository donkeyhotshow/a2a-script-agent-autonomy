output/

node_modules/

# Vite / Vitest cache under node_modules (e.g. results.json)
node_modules/.vite/

dist/

# Project-specific log folders
**/logs/

# Legacy root-level logs (to be cleaned up)
*.log

# Runtime state - PIDs change on every application start

# Session/storage in project folder (legacy - now uses ~/.a2a-client)

.pids.txt
# Sim invoke capture (merge into response.json for goldens; see simulations/SCHEMA.md)
simulations/**/invoke-capture.json
.env*

# Local operator cheatsheet only — never commit real keys (see a2a-ai-hub/FREE_LLM_KEYS.md)
**/FREE_LLM_KEYS.local.md

# Vitest JSON scratch from human-review runners
**/tests/human-review/.last-vitest.json
tests/proba-servera/**/output.json


# Direct-tests: runtime session/promise IDs for cleanup-artifacts (copy from artifacts-registry.json.example)
tests/direct-tests/artifacts-registry.json
