output/

node_modules/

dist/

# Project-specific log folders
**/logs/

# Legacy root-level logs (to be cleaned up)
*.log

# Runtime state - PIDs change on every application start

# Session/storage in project folder (legacy - now uses ~/.a2a-client)

.pids.txt
a2a-client/storage/

# Sim invoke capture (merge into response.json for goldens; see simulations/SCHEMA.md)
simulations/**/invoke-capture.json
.env*
!.env.example

# Local agent extract tree (not part of product source)
.agentLOGIC/

# Task Monitor runtime state (machine-local)
task-monitor-state.json

# Monitor run artifacts (runtime output, not source)
monitor-artifacts/

# TypeScript / tooling scratch dumps (regenerate locally if needed)
tsc-errors.txt
tsc-out.txt
tsc-out2.txt
git_status_full.txt
diff_summary.txt
dedup_report_remote.md

# Build artifacts
**/tsconfig.tsbuildinfo
**/next-env.d.ts
a2a-server/build_errors.txt
a2a-server/$log

# SHA checksum tracking files (generated, not source)
root-sha.txt
shared-sha.txt
src-sha.txt

# Test / debug scratch files
a2a-ai-hub/response.json
a2a-ai-hub/test_output.txt
tests/**/_tmp-*.json
tests/**/_debug-*.mjs

# Cursor hook ticker (regenerated every N minutes locally)
hooks/CURSOR_AGENT_SIGNAL.json
hooks/CURSOR_AGENT_SIGNAL.md

# Vitest JSON scratch from human-review runners
**/tests/human-review/.last-vitest.json

# Next.js build output
.next/
