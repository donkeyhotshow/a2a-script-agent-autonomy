проблема номер один - отображение сессий диалогов , в C:\workspace\org-carrier\a2a-script-agent\a2a-client\web , в рамках всей системы обмена данными между клиентом и сервером.

сделать переключатель , который меняет хранение сессий в папке проекта , меняет на `a2a-client/storage/sessions/` (вне проекта, можно переопределить через `A2A_CLIENT_STORAGE_DIR`).

сессия долна состоять из пронумерованых папок , от 1 , в которых должны быть отдельные части шагов системы . также отдельно в файле сохраняется тексты шага ,ето позволит , с веб панели , при обновлении диалога , запрашивать выдачу начиная с того номера сообщения , какое последнее в той сесии. 

сейчас нововведение . сначало создается сессия, а потом уже можно с ней взаимодействоват. значить сервер веб интерфейса , должен создать сессию и сразу туда прописать execute form input , чтоб можно было ввести таск . ето помент создания сессии . на втором шаге , сервер веб склиента , будет отправлять на сервер запрос , чтоб тот сделал свою работу.

пока сервер клиента , ждет от сервера ответ , на веб клиент в сессию надо командывать отображать прелоадер (через SessionStore events `promisePending` / `wait` и AppTask.setupLoaderIndicator) 


обновление ! когда клиент не будет получать message ,   а будет получать команды , не требующие ввода пользователя , тогда нужно будет создавать сообщения от имени системы и также отображать в общем диалоге . , по сути , система делает автоответ  , без веб клиента .

---

## Задачи на апгрейд кода

### 1. Система управления сессиями (a2a-client)

- [x] Реализовать переключатель для смены места хранения сессий: текущая папка проекта vs `a2a-client/storage/sessions/`
- [x] Изменить структуру хранения сессий - использовать пронумерованные папки (1/, 2/, 3/) вместо текущего формата
- [x] Каждая папка (номер) должна содержать отдельные файлы для каждого шага системы
- [x] Отдельный файл шага сохранять текст шага , для возможности запрашивать историю с определенного номера сообщения
- [x] Реализовать создание сессии с автоматическим добавлением execute form input (момент создания сессии - шаг 1)
- [x] На втором шаге сервер веб-клиента отправляет запрос на a2a-server для выполнения работы
- [x] Пока a2a-server обрабатывает запрос, отображать прелоадер на веб-клиенте (через SessionStore events `promisePending` / `wait`)

### 2. SessionStore (a2a-client/web/js/session-store.js)

- [x] Добавить функционал персистентности сессий в `a2a-client/storage/sessions/`
- [x] Реализовать переключатель между хранением в памяти и файловой системе (`a2a-client/storage/sessions/`)
- [x] Добавить метод для получения истории с определенного номера сообщения
- [x] Реализовать автосохранение состояния после каждого шага

### 3. Storage API (a2a-client/web/js/storage.js)

- [x] Расширить storage.js для поддержки пронумерованных папок сессий
- [x] Добавить методы для работы с шагами сессии (createStep, getStep, listSteps)
- [x] Реализовать формат хранения: sessions/{sessionId}/1/, sessions/{sessionId}/2/ и т.д.

### 4. Web UI компоненты

- [x] Добавить UI переключатель storage mode (проект vs `a2a-client/storage/sessions/`)
- [x] Обновить отображение сессий для работы с пронумерованными папками
- [x] Добавить индикатор прелоадера при ожидании ответа от a2a-server

### 5. API интеграция

- [x] Добавить эндпоинт для создания сессии с начальным execute form input
- [x] Добавить эндпоинт для получения истории сессии с определенного номера
- [x] Добавить эндпоинт для проверки статуса обработки (прелоадер)

---

## Текущие исправления (2026-03-12)

### Исправленные баги

- [x] A2A Server endpoints: `/invoke` → `/api/v1/invoke`
- [x] Invoke service: исправлен приоритет result в `invoke.service.ts`
- [x] Promise routes: добавлена проверка статуса Ollama в `promise_routes.py`
- [x] Session storage: добавлено сохранение истории сообщений в `session.json`

---

## Задачи на документы

### 1. Архитектурная документация (a2a-client/docs/)

- [x] Обновить документацию по архитектуре сессий
- [x] Добавить документацию по формату хранения сессий в `a2a-client/storage/sessions/`
- [x] Обновить ADR (Architecture Decision Records) с новой структурой сессий

### 2. API документация

- [x] Обновить a2a-client/docs/tester/API.md
- [x] Добавить примеры использования Session Storage API

### 3. Интеграционная документация

- [x] Обновить workflow документацию (a2a-client/docs/workflows/)
- [x] Добавить гайд по миграции на новую систему хранения сессий

---

## Актуальное состояние проекта

### Реализованные функции

1. **Система управления сессиями** - полностью реализована
2. **SessionStore с персистентностью** - работает с `a2a-client/storage/sessions/`
3. **Storage API** - поддержка пронумерованных папок
4. **Web UI компоненты** - переключатель storage mode и индикатор загрузки
5. **API интеграция** - все эндпоинты работают

### Технические детали

- A2A Server работает на порту 3000
- Client API работает на порту 3001
- Web UI работает на порту 5173
- Ollama используется для AI-обработки (порт 11434)
- Сессии хранятся в пронумерованных папках (1/, 2/, 3/, ...)
- История сообщений сохраняется в session.json
