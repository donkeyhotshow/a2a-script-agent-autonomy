# Plan: Fix npm run dev script in a2a-client

## Goal
Fix the `npm run dev` command in `a2a-client/package.json` by removing the call to the missing `..\scripts\start-web-ui.bat` and replacing it with a working command to start the development server.

## Analysis
- Current dev script: `"dev": "..\\scripts\\start-web-ui.bat"`
- The batch file `scripts/start-web-ui.bat` does not exist, causing the command to fail.
- Alternative script exists: `"dev:local": "vite"`
- Vite is used for building and serving the web UI (vanilla JS + VueFlow).

## Proposed Solution
Change the dev script to use Vite directly, similar to dev:local.

## Steps
1. Edit `a2a-client/package.json` line 22: change `"dev": "..\\scripts\\start-web-ui.bat",` to `"dev": "vite",`

## Verification
- Run `npm run dev` in a2a-client directory to confirm it starts the dev server successfully.

## Risks
- Ensure Vite configuration is correct for the project.
- Check if the batch file was doing additional setup (unlikely, as it doesn't exist).</content>
<parameter name="filePath">C:\workspace\org-carrier\a2a-script-agent\.kilo\plans\1775840446650-glowing-tiger.md