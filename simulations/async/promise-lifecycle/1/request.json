{
  "context": {
    "task": "Golden: async promise lifecycle (poll then finalize).",
    "execution": {
      "action": "task",
      "step": "new"
    }
  },
  "result": {
    "message": "Golden: async promise lifecycle (poll then finalize)."
  }
}
