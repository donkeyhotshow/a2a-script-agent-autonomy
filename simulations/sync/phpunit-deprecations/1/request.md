# `phpunit-deprecations/1` — request

Mirror of `request.json` for prompt pipeline / `sim:check-md`.

```json
{
  "context": {
    "task": "знайти застарілі PHPUnit методи",
    "execution": {
      "action": "task",
      "step": "new"
    }
  },
  "result": {
    "message": "знайти застарілі PHPUnit методи"
  }
}
```
