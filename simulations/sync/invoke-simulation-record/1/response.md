# `invoke-simulation-record/1` — copy of `response.json` for drift checks

**Not model output.** Fixture mirror only (`sim:check-md`).

```json
{
  "context": {
    "task": "Golden: SimulationRequestProcessor record mode.",
    "execution": {
      "action": "task",
      "step": "sim_record"
    },
    "simulation_name": "invoke-simulation-record"
  },
  "execute": {
    "message": "Started simulation: invoke-simulation-record"
  }
}
```
