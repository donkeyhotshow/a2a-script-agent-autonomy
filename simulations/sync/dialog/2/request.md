# `dialog/2` — request (router choice `dialog`)

Mirror of `request.json` for prompt pipeline / `sim:check-md`.

```json
{
  "context": {
    "task": "диалог",
    "execution": {
      "action": "task",
      "step": "router"
    }
  },
  "result": {
    "choice": "dialog"
  }
}
```
