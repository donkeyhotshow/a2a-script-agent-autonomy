# `form-validation-error/3` — request

Mirror of `request.json` for prompt pipeline / `sim:check-md`.

```json
{
  "context": {
    "task": "форма з помилкою валідації",
    "execution": {
      "action": "dialog",
      "step": "request"
    }
  },
  "result": {
    "input": {
      "age": 5
    }
  }
}
```
