# Step 15 — request (User requests commit)

User asks to commit the changes.

```json
{
  "context": { "execution": { "action": "dialog", "step": "summary" } },
  "result": { "message": "Спасибо, отличная работа! Можешь закоммитить эти изменения?" }
}
```