# Step 14 — request (User requests session summary)

User asks for a summary of everything done in the session.

```json
{
  "context": { "execution": { "action": "dialog", "step": "completed" } },
  "result": { "message": "Спасибо, всё отлично! Можешь показать итоговую сводку того, что мы сделали?" }
}
```