# `fix-laravel-namespaces-and-uses/1` — request

Mirror of `request.json` for prompt pipeline / `sim:check-md`.

```json
{
  "context": {
    "task": "виправити namespace та use у Laravel PHP файлах",
    "execution": {
      "action": "task",
      "step": "new"
    }
  },
  "result": {
    "message": "виправити namespace та use у Laravel PHP файлах"
  }
}
```
