# `agent-coder-smart/2` — copy of `response.json` for drift checks

**Not model output.** Same as `request.md`: fixture mirror only (`sim:check-md`), not part of the LLM pipeline on steps 1–2.

```json
{
  "context": {
    "task": "створи задачу і виконай",
    "execution": {
      "action": "agent",
      "step": "user-request"
    },
    "history": []
  },
  "execute": {
    "form": {
      "input": [
        {
          "name": "task",
          "type": "text",
          "label": "Повідомлення",
          "required": true
        }
      ]
    }
  }
}
```