# `agent-auto-ai/2` — request

Mirror of `request.json` for prompt pipeline / `sim:check-md`.

```json
{
  "context": {
    "task": "Add GET /health returning JSON { ok: true }; wire the route in src/app.js.",
    "execution": {
      "action": "task",
      "step": "router"
    }
  },
  "result": {
    "router": {
      "choice": "agent"
    }
  }
}
```
