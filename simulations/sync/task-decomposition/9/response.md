# `task-decomposition/9` — copy of `response.json` for drift checks

**Not model output.** Same as `request.md`: fixture mirror only (`sim:check-md`), not part of the LLM pipeline.

```json
{
  "context": {
    "task": "add auth and refactor API",
    "execution": {
      "action": "task-decomposition",
      "step": "execute-action"
    },
    "history": []
  },
  "execute": {
    "form": {
      "input": [
        {
          "name": "task",
          "type": "text",
          "label": "Task doc content",
          "required": true
        }
      ]
    }
  }
}
```