# `router-rejection/3` — request

Mirror of `request.json` for prompt pipeline / `sim:check-md`.

```json
{
  "context": {
    "task": "тест обробки неправильного вибору",
    "execution": {
      "action": "task",
      "step": "router"
    }
  },
  "result": {
    "choice": "valid-choice"
  }
}
```
