# `orchestrator-dialog/1` — request

Mirror of `request.json` for prompt pipeline / `sim:check-md`.

```json
{
  "context": {
    "task": "orchestrate ADR review",
    "history": [],
    "execution": {
      "action": "task",
      "step": "new"
    }
  },
  "result": {
    "message": "Orchestrate ADR review and plan the audit."
  }
}
```
