# `dialog-interrupt/1` — request

Mirror of `request.json` for prompt pipeline / `sim:check-md`.

```json
{
  "context": {
    "task": "Dialog interrupt golden — when clause vs short history",
    "execution": {
      "action": "dialog",
      "step": "message-only"
    },
    "history": [
      {
        "role": "user",
        "message": "One user line only."
      }
    ]
  }
}
```
