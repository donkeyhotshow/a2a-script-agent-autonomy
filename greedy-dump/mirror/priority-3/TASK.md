# Task: priority-3 → script-agent

**Why:** Largest curated app set—scripts must be triaged **per subfolder** (see `SUBTREE.md`).

**Rule:** One implementation pass per subproject after shared patterns exist from priority-1/2.
