# Subtask: start-session

**Contents:** Session bootstrap prompts or scripts.

**Integration:** Align with Client API session create (`POST /api/a2a/sessions`); avoid duplicating router logic—reuse `mode`/`task` seeding.
