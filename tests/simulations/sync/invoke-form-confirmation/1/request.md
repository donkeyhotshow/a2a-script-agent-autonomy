# `invoke-form-confirmation/1` — request

Mirror of `request.json` for prompt pipeline / `sim:check-md`.

```json
{
  "context": {
    "task": "Golden: FormRequestProcessor confirmation submit.",
    "execution": {
      "action": "task",
      "step": "form_submit"
    },
    "form_id": "confirmation",
    "form_submission": {}
  }
}
```
