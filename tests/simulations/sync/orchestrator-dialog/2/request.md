# `orchestrator-dialog/2` — request

Mirror of `request.json` for prompt pipeline / `sim:check-md`.

```json
{
  "context": {
    "task": "orchestrate ADR review",
    "history": [],
    "execution": {
      "action": "task",
      "step": "router"
    }
  },
  "result": {
    "choice": "orchestrator-dialog"
  }
}
```
