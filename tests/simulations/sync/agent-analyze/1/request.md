# `agent-analyze/1` — request

Mirror of `request.json` for prompt pipeline / `sim:check-md`.

```json
{
  "context": {
    "task": "аналіз",
    "execution": {
      "action": "task",
      "step": "new"
    }
  },
  "result": {
    "message": "аналіз"
  }
}
```
