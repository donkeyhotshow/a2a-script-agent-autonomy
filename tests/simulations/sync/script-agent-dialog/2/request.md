# Step 2 — request (router choice)

Mirror of request.json (choose dialog).

```json
{
  "context": {
    "task": "script-agent-dialog: auto run fix-vue-imports when agent flags import errors.",
    "execution": {
      "action": "task",
      "step": "router"
    },
    "workbench": {
      "sections": {}
    }
  },
  "result": {
    "choice": "dialog"
  }
}
```
