# `task-decomposition/3` — request

Mirror of `request.json` for prompt pipeline / `sim:check-md`.

```json
{
  "context": {
    "task": "add auth and refactor API",
    "execution": {
      "action": "task-decomposition",
      "step": "task-decomposition"
    },
    "history": []
  },
  "result": {
    "message": "add auth and refactor API"
  }
}
```
