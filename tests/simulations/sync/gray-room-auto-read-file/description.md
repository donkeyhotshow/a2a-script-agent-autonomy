# Gray room — `auto_read_file` interrupt (dialog)

Golden for **`interrupt.reason: "auto_read_file"`** with `data.path` / `data.filePath`: server merges file content into **`context.files`**, `continueLoop: false`. See [`GRAY-ROOM.md`](../../../a2a-server/docs/GRAY-ROOM.md).
