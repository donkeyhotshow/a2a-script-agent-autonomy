# `fix-vue-imports-decline/1` — request

Mirror of `request.json` for prompt pipeline / `sim:check-md`.

```json
{
  "context": {
    "task": "виправити імпорти у vue компонентах",
    "execution": {
      "action": "task",
      "step": "new"
    }
  },
  "result": {
    "message": "виправити імпорти у vue компонентах"
  }
}
```
