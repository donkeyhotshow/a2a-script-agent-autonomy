# `invoke-simulation-record/1` — request

Mirror of `request.json` for prompt pipeline / `sim:check-md`.

```json
{
  "context": {
    "task": "Golden: SimulationRequestProcessor record mode.",
    "execution": {
      "action": "task",
      "step": "sim_record"
    },
    "simulation_name": "invoke-simulation-record"
  }
}
```
