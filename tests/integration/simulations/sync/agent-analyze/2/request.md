# `agent-analyze/2` — request

Mirror of `request.json` for prompt pipeline / `sim:check-md`.

```json
{
  "context": {
    "task": "аналіз",
    "execution": {
      "action": "task",
      "step": "router"
    }
  },
  "result": {
    "choice": "analyze"
  }
}
```
