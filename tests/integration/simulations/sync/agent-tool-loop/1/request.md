# `agent-tool-loop/1` — request

Mirror of `request.json` for prompt pipeline / `sim:check-md`.

```json
{
  "context": {
    "task": "зчитати файл і знайти функцію",
    "execution": {
      "action": "task",
      "step": "router"
    }
  }
}
```
