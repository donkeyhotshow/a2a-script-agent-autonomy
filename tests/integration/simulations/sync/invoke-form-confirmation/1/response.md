# `invoke-form-confirmation/1` — copy of `response.json` for drift checks

**Not model output.** Fixture mirror only (`sim:check-md`).

```json
{
  "context": {
    "task": "Golden: FormRequestProcessor confirmation submit.",
    "execution": {
      "action": "task",
      "step": "form_submit"
    },
    "form_id": "confirmation",
    "form_submission": {}
  },
  "execute": {
    "message": "Form \"Підтвердження\" submitted successfully"
  }
}
```
