# `agent-coder/1` — request

Mirror of `request.json` for prompt pipeline / `sim:check-md`.

```json
{
  "context": {
    "task": "допоможи розібратись з кодом",
    "execution": {
      "action": "task",
      "step": "new"
    }
  },
  "result": {
    "message": "допоможи розібратись з кодом"
  }
}
```
