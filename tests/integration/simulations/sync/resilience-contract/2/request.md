# `resilience-contract/2` — request

Mirror of `request.json` for prompt pipeline / `sim:check-md`.

```json
{
  "context": {
    "task": "Golden: resilience contract (paginated RAG drain, read-file queue, human gate).",
    "execution": {
      "action": "agent",
      "step": "human_gate"
    },
    "workbench": {
      "sections": {}
    },
    "history": []
  },
  "result": {
    "choice": "proceed_resilience"
  }
}
```
