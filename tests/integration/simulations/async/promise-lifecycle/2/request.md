# `promise-lifecycle/2` — request

Mirror of `request.json` for prompt pipeline / `sim:check-md`.

```json
{
  "context": {
    "task": "Golden: async promise lifecycle (poll then finalize).",
    "execution": {
      "action": "task",
      "step": "processing"
    }
  }
}
```
