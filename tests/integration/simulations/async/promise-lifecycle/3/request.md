# `promise-lifecycle/3` — request

Mirror of `request.json` for prompt pipeline / `sim:check-md`.

```json
{
  "context": {
    "task": "Golden: async promise lifecycle (failed terminal).",
    "execution": {
      "action": "task",
      "step": "processing"
    }
  }
}
```
