{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Validation rules for Port Single Editor fields",
  "fields": {
    "port": {
      "type": "integer",
      "minimum": 1,
      "maximum": 65535,
      "required": true,
      "errorMessages": {
        "required": "Port number is required",
        "type": "Port number must be an integer",
        "minimum": "Port number must be at least 1",
        "maximum": "Port number must be at most 65535"
      }
    },
    "protocol": {
      "type": "string",
      "enumRef": "enums.protocols",
      "required": true,
      "errorMessages": {
        "required": "Protocol is required",
        "type": "Protocol must be a string",
        "enum": "Invalid protocol value"
      }
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 100,
      "required": true,
      "errorMessages": {
        "required": "Service name is required",
        "minLength": "Service name must be at least 1 character long",
        "maxLength": "Service name must be at most 100 characters long"
      }
    },
    "description": {
      "type": "string",
      "maxLength": 500,
      "errorMessages": {
        "maxLength": "Description must be at most 500 characters long"
      }
    },
    "isEnabled": {
      "type": "boolean",
      "required": true,
      "errorMessages": {
        "required": "Enabled status is required",
        "type": "Enabled status must be a boolean"
      }
    }
  }
}
