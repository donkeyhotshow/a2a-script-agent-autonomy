{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Validation rules for Port Conflict Resolver fields",
  "fields": {
    "conflictingPort": {
      "type": "integer",
      "minimum": 1,
      "maximum": 65535,
      "required": true,
      "errorMessages": {
        "required": "Conflicting port is required",
        "type": "Conflicting port must be an integer",
        "minimum": "Conflicting port must be at least 1",
        "maximum": "Conflicting port must be at most 65535"
      }
    },
    "resolutionOption": {
      "type": "string",
      "enumRef": "enums.resolutionOptions",
      "required": true,
      "errorMessages": {
        "required": "Resolution option is required",
        "type": "Resolution option must be a string",
        "enum": "Invalid resolution option"
      }
    }
  }
}
