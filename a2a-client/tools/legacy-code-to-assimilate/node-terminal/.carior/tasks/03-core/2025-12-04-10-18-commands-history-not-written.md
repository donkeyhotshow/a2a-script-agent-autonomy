# Проблема записи глобальной истории команд MCP Node Terminal

## Дата создания
2025-12-04 10:18:00

## Исходный запрос пользователя
почему история команд не пишется  ?

## Улучшенная формулировка
Разобраться и устранить проблему отсутствия записи истории команд в MCP Node Terminal (файл `data/global-history/commands.jsonl`) для повышения прозрачности и управляемости терминальных операций.

Требования:
- [ ] Проанализировать текущую архитектуру и конфигурацию записи истории команд MCP Node Terminal.
- [ ] Найти причину, по которой новые команды не попадают в `data/global-history/commands.jsonl`.
- [ ] Разработать и реализовать корректное решение (фикс/настройку), чтобы история команд стабильно писалась для MCP терминала.
- [ ] Обновить/добавить тесты, покрывающие сценарии записи истории команд.
- [ ] Задокументировать поведение и возможные ограничения/настройки истории команд.

Ожидаемый результат:
История команд MCP Node Terminal стабильно записывается в `data/global-history/commands.jsonl` в соответствии с архитектурными требованиями, есть тестовое покрытие и понятная документация по настройкам и ограничениям.

## Контекст
В проекте существует файл `data/global-history/commands.jsonl`, а также тесты и скрипты, связанные с глобальной историей (`tests/history/global-history-structure.test.cjs`, `test-global-history.cjs`). Пользователь замечает, что история команд не пополняется новыми записями, несмотря на использование MCP терминала. Необходимо сверить фактическую реализацию записи истории с требованиями из документации и тестов, а также убедиться, что MCP-терминал интегрирован с системой глобальной истории корректно.

## Фаза проекта
- [ ] 01-init
- [x] 03-core
- [ ] 05-development
- [ ] 07-testing
- [ ] 09-optimization

## Требования
- [ ] Понять, при каких условиях и через какие модули MCP Node Terminal должен записывать глобальную историю команд.
- [ ] Проверить связь MCP-обработчиков терминала с механизмом записи в `data/global-history/commands.jsonl`.
- [ ] Выявить конфигурационные флаги/режимы, которые могут отключать или изменять поведение записи истории.
- [ ] Обеспечить, чтобы MCP-терминал (через MCP-интеграцию в Cursor) корректно инициировал запись истории.
- [ ] Обновить тесты (в `tests/history` и/или `tests/unit`) для проверки записи истории при выполнении MCP-команд.
- [ ] Исключить регрессии в механизмах истории (локальная история, глобальная история, сессии).

## Технические ограничения
- Использовать только MCP terminal (`mcp_node-terminal_terminal`) для любых проверок и команд.
- Не нарушать существующую архитектуру logging/history без явной необходимости.
- Все сценарии проверки должны быть оформлены как тесты в `tests/` в соответствии с правилами `.cursor/rules/tests-conversion.mdc` и `.cursor/rules/scripts-and-tests-conversion.mdc`.
- Не изменять версии зависимостей без отдельного цикла миграции.

## Ожидаемый результат
После выполнения задачи:
- История команд MCP терминала записывается в `data/global-history/commands.jsonl` при выполнении команд через MCP.
- Причина текущего отсутствия записи задокументирована.
- Есть тесты, которые воспроизводят сценарий и гарантируют отсутствие регрессий.

## Решение
_Будет заполнено после анализа и реализации фикса._

## Результат
_Будет заполнено после завершения работ._

## Статус
- В процессе

## Связанные файлы
- `data/global-history/commands.jsonl`
- `tests/history/global-history-structure.test.cjs`
- `tests/unit/terminal-handler-history-core.test.cjs`
- `tests/unit/terminal-handler-history.test.cjs`
- `test-global-history.cjs`


