# Улучшение обертки MCP сервера с отслеживанием состояния

## Дата создания
2025-11-29 01:14:00

## Исходный запрос пользователя
сейчас mcp_node-terminal_terminal - в дауне . самое время проверить обертку , которая обязана стартовать и давать тулзы , но с ошибкой , пока сервер не подимется . еще , в случае падения сервера , надо какойто файл задачи , чтоб обновлялся , чтоб было видно , что происходит на фоне

## Улучшенная формулировка
Улучшить обертку MCP сервера (`server-wrapper-simple.cjs`) для отслеживания состояния сервера через файл задачи в `.carior/tasks/`, который обновляется в реальном времени при изменениях состояния (старт, падение, перезапуск, ошибки).

**Архитектура обертки:**
- Обертка инициализирует тулзы и проксирует их в MCP сервер
- Когда MCP сервер не работает, обертка отправляет ответ, что тул недоступен
- При недоступности тула работа над задачей должна быть остановлена

**Требования:**
- [x] Обертка должна обновлять файл статуса `logs/wrapper-status.json` при каждом изменении состояния
- [x] Создать и обновлять файл задачи в `.carior/tasks/01-init/` с информацией о текущем состоянии сервера
- [x] Файл задачи должен обновляться при: старте сервера, падении, перезапуске, ошибках
- [x] Файл задачи должен содержать: текущий статус, PID процесса, количество попыток, последнюю ошибку, время последнего события
- [x] Обертка должна работать даже когда сервер падает (показывать ошибки в файле задачи)
- [x] Файл задачи должен быть читаемым и информативным для диагностики
- [ ] Обертка должна инициализировать тулзы и проксировать их в MCP сервер
- [ ] Когда MCP сервер не работает, обертка должна возвращать ошибку о недоступности тула
- [ ] При получении ошибки о недоступности тула работа над задачей должна быть остановлена

## Контекст
- MCP сервер `node-terminal` падает с ошибкой `MODULE_NOT_FOUND`
- Обертка `server-wrapper-simple.cjs` работает, но не обновляет файл статуса (в отличие от полной версии)
- Нет механизма отслеживания состояния через файл задачи
- Пользователь хочет видеть, что происходит на фоне, когда сервер падает
- Обертка инициализирует тулзы и проксирует их в MCP сервер
- Когда MCP сервер не работает, обертка должна возвращать ошибку о недоступности тула
- При недоступности тула работа над задачей должна быть остановлена (см. правило `.cursor/rules/mcp-terminal-mandatory.mdc`)

## Фаза проекта
01-init (инициализация и запуск процессов)

## Требования
- [x] Добавить функцию обновления статуса в `server-wrapper-simple.cjs`
- [x] Создать функцию обновления файла задачи в `.carior/tasks/01-init/`
- [x] Интегрировать обновление статуса и файла задачи во все события (старт, падение, перезапуск, ошибки)
- [x] Обеспечить атомарность записи (безопасная запись файлов)
- [x] Добавить обработку ошибок при записи файлов (не должно падать обертку)

## Технические ограничения
- Обертка должна работать в Windows PowerShell окружении
- Файлы должны быть читаемыми и не блокировать работу обертки
- Запись должна быть безопасной (не должно ломать обертку при ошибках)
- Формат файла задачи - Markdown для читаемости

## Ожидаемый результат
- Обертка обновляет `logs/wrapper-status.json` при каждом изменении состояния
- Файл задачи `.carior/tasks/01-init/wrapper-server-status.md` создается и обновляется в реальном времени
- Пользователь может видеть текущее состояние сервера, читая файл задачи
- При падении сервера файл задачи показывает ошибку и количество попыток перезапуска

## Решение
1. Добавить функцию `updateStatus()` в `server-wrapper-simple.cjs` для обновления JSON статуса
2. Добавить функцию `updateTaskFile()` для обновления Markdown файла задачи
3. Интегрировать вызовы этих функций во все события обертки
4. Обеспечить безопасную запись с обработкой ошибок

## Результат
Обертка `server-wrapper-simple.cjs` улучшена с добавлением механизма отслеживания состояния:

1. **Добавлена функция `updateStatus()`** - обновляет JSON файл `logs/wrapper-status.json` с текущим состоянием обертки
2. **Добавлена функция `updateTaskFile()`** - создает и обновляет Markdown файл `.carior/tasks/01-init/wrapper-server-status.md` с информацией о состоянии сервера
3. **Интегрировано обновление статуса** во все события:
   - При старте сервера (state: 'running')
   - При падении сервера (state: 'error')
   - При перезапуске (state: 'restarting')
   - При ошибках запуска (state: 'error')
   - При нормальном завершении (state: 'stopped')
4. **Обеспечена безопасная запись** - все операции записи обернуты в try-catch, ошибки не падают обертку
5. **Файл задачи содержит**:
   - Текущий статус с эмодзи-индикаторами
   - PID процесса
   - Количество попыток перезапуска
   - Последнюю ошибку (если есть)
   - Время последнего события
   - Ссылки на файлы для диагностики

Теперь пользователь может отслеживать состояние сервера в реальном времени, читая файл `.carior/tasks/01-init/wrapper-server-status.md`.

**Архитектура обертки:**
- Обертка инициализирует тулзы и проксирует их в MCP сервер через stdin/stdout
- Когда MCP сервер не работает (не отвечает на запросы), обертка должна возвращать ошибку о недоступности тула
- При получении ошибки о недоступности тула работа над задачей должна быть остановлена (см. правило `.cursor/rules/mcp-terminal-mandatory.mdc`)

## Статус
Завершено (обновлено с учетом архитектуры обертки и правил остановки работы)

## Обновления
- **2025-12-XX**: Добавлена информация об архитектуре обертки (инициализация тулзов, проксирование в MCP сервер)
- **2025-12-XX**: Добавлено требование остановки работы при недоступности тула
- **2025-12-XX**: Обновлено правило `.cursor/rules/mcp-terminal-mandatory.mdc` с инструкциями по обработке недоступности тула

## Связанные файлы
- `scripts/wrapper/server-wrapper-simple.cjs` - обновлен (добавлено отслеживание статуса и рекомендации)
- `logs/wrapper-status.json` - создан/обновлен
- `.carior/tasks/01-init/wrapper-server-status.md` - создан (автоматически обновляется)
- `.cursor/rules/mcp-terminal-mandatory.mdc` - обновлен (добавлены правила обработки недоступности тула и остановки работы)
- `mcp/server/Validation.cjs` - исправлен (добавлена обработка ошибки для setup-module-alias.cjs)
- `mcp/TestGetter.cjs` - исправлен (добавлена обработка ошибки)
- `mcp/TestInterceptor.cjs` - исправлен (добавлена обработка ошибки)
- `mcp/DebugSystem.cjs` - исправлен (добавлена обработка ошибки)

