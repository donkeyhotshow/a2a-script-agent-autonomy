# Обновление точек входа и конфигурации MCP Terminal в README

## Дата создания
2025-12-04 10:05:00

## Исходный запрос пользователя
в @README.md  , к точкам входа , добавь C:\apps\root\mcp\node-terminal\.cursor\mcp.json - надо менять абсолютный путь , если неверный путь . , C:\apps\root\mcp\node-terminal\.cursor\rules\mcp-terminal-mandatory.mdc

## Улучшенная формулировка
Обновить описание точек входа MCP Terminal в `README.md` для явного указания конфигурационного файла `.cursor/mcp.json` (с напоминанием менять абсолютный путь при необходимости) и обязательного правила `.cursor/rules/mcp-terminal-mandatory.mdc`.

## Контекст
Документация уже упоминает `.cursor/mcp.json` в разделе про wrapper, но не даёт явной ссылочной точки в блоке «Точки входа», где пользователь ожидает увидеть полную картину: входные файлы сервера и связанные с ними конфигурации/правила Cursor. Требуется сделать документацию более предсказуемой и самодостаточной.

## Фаза проекта
- [ ] 01-init
- [ ] 02-foundation
- [ ] 03-core
- [ ] 04-architecture
- [ ] 05-development
- [x] 06-documentation
- [ ] 07-testing
- [ ] 08-security
- [ ] 09-optimization
- [ ] 10-deployment

## Требования
- [x] Добавить в раздел `Точки входа` README явное описание файла конфигурации `.cursor/mcp.json` с указанием, что абсолютный путь к проекту нужно проверять и при необходимости менять.
- [x] Добавить ссылку/упоминание обязательного правила `.cursor/rules/mcp-terminal-mandatory.mdc` как части конфигурации для Cursor.
- [x] Сохранить существующую структуру и стиль раздела, не ломая текущие ссылки и форматирование.

## Технические ограничения
- Не изменять поведение кода, только текстовую документацию.
- Сохранить совместимость с текущей конфигурацией `.cursor/mcp.json`.
- Не добавлять новые зависимости и не изменять версию пакетов.

## Ожидаемый результат
В `README.md` в разделе «Точки входа» явно описаны:
- файлы `mcp-server.cjs` и `server-wrapper-simple.cjs` как точки входа MCP-сервера;
- конфигурационный файл `.cursor/mcp.json` с пояснением про корректировку абсолютного пути;
- обязательное правило `.cursor/rules/mcp-terminal-mandatory.mdc` как часть конфигурации Cursor IDE.

## Решение
- Обновлён раздел «Точки входа» в `README.md` с добавлением подпункта про конфигурацию Cursor IDE:
  - указание пути к `.cursor/mcp.json` и напоминание, что абсолютный путь к проекту нужно менять, если он отличается;
  - упоминание файла правил `.cursor/rules/mcp-terminal-mandatory.mdc` как обязательного для настройки.

## Результат
- [x] Обновлён текст в `README.md` в разделе «Точки входа» с явной фиксацией `.cursor/mcp.json` и `.cursor/rules/mcp-terminal-mandatory.mdc`.

## Статус
- Завершено

## Связанные файлы
- `README.md` - обновлён






