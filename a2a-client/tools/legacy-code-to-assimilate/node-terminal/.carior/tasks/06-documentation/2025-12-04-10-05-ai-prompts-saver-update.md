# Обновление правила ai-prompts-saver: возврат PowerShell-команд для даты/времени

## Дата создания
2025-12-04 10:05:18

## Исходный запрос пользователя
@.cursor/rules/ai-prompts-saver.mdc  - обновить , потому что , carior session , небудет доступна ета команда . надо вернеть павершеловские команды на получения дат . пичи команду пайпом

## Улучшенная формулировка
Обновить правило `ai-prompts-saver` в `.cursor/rules/ai-prompts-saver.mdc` для использования стандартных PowerShell-команд получения даты/времени вместо недоступной команды `carior session`, добавив пример команды с пайпом.

**Требования:**
- [x] Определить текущее положение разделов про `carior session` и fallback-механику получения даты/времени в `ai-prompts-saver.mdc`.
- [x] Переформулировать раздел про получение даты/времени так, чтобы основной сценарий опирался на PowerShell (`Get-Date`), без обязательной зависимости от `carior session`.
- [x] Добавить пример команды PowerShell с пайпом для генерации timestamp/даты.
- [x] Убедиться, что формат имени файла задачи и секции "Дата создания" остаётся прежним (`YYYY-MM-DD-HH-MM` и `YYYY-MM-DD HH:mm:ss`).
- [x] Обновить документ так, чтобы инструкция оставалась совместимой с текущей структурой `.carior/tasks/{phase-id}/`.

## Контекст
- Команда `carior session` в целевом окружении MCP-терминала недоступна, поэтому жёсткая зависимость от неё в правилах приводит к невыполнимым требованиям.
- В проекте уже используется fallback через PowerShell `Get-Date`, но он описан как аварийный сценарий, а не основной.
- Необходимо сделать PowerShell-алгоритм основным и самодостаточным, сохранив структуру и критерии ai-prompts-saver.

## Фаза проекта
- [ ] 01-init
- [ ] 02-foundation
- [ ] 03-core
- [ ] 04-architecture
- [ ] 05-development
- [x] 06-documentation
- [ ] 07-testing
- [ ] 08-security
- [ ] 09-optimization
- [ ] 10-deployment

## Требования
- [x] Использовать только PowerShell-команды, гарантированно доступные в целевом окружении (`Get-Date`, форматирование строк, пайпы).
- [x] Убрать формулировку "Основной сценарий — `carior session`" и описать основной сценарий через PowerShell.
- [x] Сохранить все форматные требования к именам файлов задач и полю "Дата создания".
- [x] Оставить структуру чеклистов и критериев успешного сохранения без изменений.
- [x] Обновить документацию без нарушения других правил и ссылок в файле.

## Технические ограничения
- Используем только PowerShell (MCP terminal работает в PowerShell).
- Нельзя полагаться на наличие `carior` CLI.
- Нельзя нарушать текущую структуру `.carior/tasks` и формат имени файлов задач.

## Ожидаемый результат
Обновлённый файл `.cursor/rules/ai-prompts-saver.mdc`, в котором:
- основной алгоритм получения даты/времени описан через PowerShell `Get-Date`;
- приведён пример команды с пайпом для формирования timestamp;
- блок про `carior session` либо удалён, либо явно помечен как опциональный/нестандартный и не являющийся обязательным шагом;
- все остальные разделы правила сохранены и остаются актуальными.

## Решение
- Проанализировать текущий текст `ai-prompts-saver.mdc`, найти разделы:
  - "**Получение даты и времени (fallback механизм)**";
  - "Команда `carior session` — обязательный шаг старта".
- Сделать PowerShell-алгоритм (`Get-Date` + форматирование) основным механизмом, описав его как стандартный шаг.
- Переписать/сузить блок про `carior session`, указав, что это опциональный инструмент (если когда-то доступен), но не обязательное требование.
- Добавить пример PowerShell-команды с пайпом, например:
  - `Get-Date | ForEach-Object { $_.ToString("yyyy-MM-dd-HH-mm") }`
  - и/или запись в файл.
- Обновить документ аккуратным патчем без изменения других смысловых частей.

## Результат
- [x] Добавлен файл-задача `.carior/tasks/06-documentation/2025-12-04-10-05-ai-prompts-saver-update.md`.
- [x] Обновлён файл `.cursor/rules/ai-prompts-saver.mdc` согласно требованиям: основной сценарий получения даты/времени описан через PowerShell `Get-Date` с примером команды с пайпом, блок про `carior session` как обязательный шаг старта удалён.

## Статус
- Завершено

## Связанные файлы
- `.cursor/rules/ai-prompts-saver.mdc` - будет обновлён
- `.carior/tasks/06-documentation/2025-12-04-10-05-ai-prompts-saver-update.md` - создан


