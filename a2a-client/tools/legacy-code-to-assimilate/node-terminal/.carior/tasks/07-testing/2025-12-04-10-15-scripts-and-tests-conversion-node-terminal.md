# Адаптация правила конвертации скриптов и сценариев в тесты под MCP Node Terminal

## Дата создания
2025-12-04 10:15:48

## Исходный запрос пользователя
@.cursor/rules/scripts-and-tests-conversion.mdc  адаптировать под етот проект

## Улучшенная формулировка
Адаптировать правило `.cursor/rules/scripts-and-tests-conversion.mdc` под текущий проект MCP Node Terminal, чтобы оно описывало конвертацию временных скриптов и сценариев проверки в тесты и поддерживаемый код именно для этого репозитория (Node/MCP, PowerShell), было согласовано с уже существующим правилом `.cursor/rules/tests-conversion.mdc` и общей стратегией тестирования.

Требования:
- [x] Проанализировать текущее содержимое `.cursor/rules/scripts-and-tests-conversion.mdc` и выявить привязки к стеку PHP/Laravel/Vitest/Playwright, которые неприменимы в MCP Node Terminal.
- [x] Учитывать и не дублировать уже существующее правило `.cursor/rules/tests-conversion.mdc`, которое описывает конвертацию сценариев MCP Terminal в тесты.
- [x] Обновить `.cursor/rules/scripts-and-tests-conversion.mdc` так, чтобы:
  - [x] Базовые принципы (нет “левых” скриптов, всё поведение → тесты/команды/CLI) сохранились.
  - [x] Примеры, структуры директорий и типы тестов были релевантны MCP Node Terminal (Node, PowerShell, структура `tests/`, возможные `scripts/`, пакетные тесты и раннеры).
  - [x] Правило было совместимо с существующей системой батчей `.carior/config/testing.config.json` и `.carior/config/testing/batches/*.batch.json`, если/когда батчи появятся для этого проекта.
- [x] Зафиксировать связь правила с фазой тестирования (07-testing) и задачами по конвертации скриптов в тесты.

Ожидаемый результат:
Обновлённый файл `.cursor/rules/scripts-and-tests-conversion.mdc`, в котором:
- описаны принципы обращения со скриптами и сценариями проверки в контексте MCP Node Terminal;
- даны шаблоны/паттерны для переноса скриптов в `tests/` или в поддерживаемые CLI/пакеты;
- устранены или адаптированы PHP/Laravel/Vitest/Playwright-специфичные части, не релевантные этому проекту;
- учтено уже существующее специализированное правило `.cursor/rules/tests-conversion.mdc` для MCP Terminal.

Фаза проекта:
- [ ] 01-init
- [ ] 02-foundation
- [ ] 03-core
- [ ] 04-architecture
- [ ] 05-development
- [ ] 06-documentation
- [x] 07-testing
- [ ] 08-security
- [ ] 09-optimization
- [ ] 10-deployment

## Контекст
- В репозитории уже есть правило `.cursor/rules/tests-conversion.mdc`, фокусирующееся на сценариях проверки MCP Terminal и их переносе в `tests/`.
- Файл `.cursor/rules/scripts-and-tests-conversion.mdc` более общий и изначально ориентирован на PHP/Laravel/Vitest/Playwright-проекты с батчами PHPUnit/Vitest/Playwright.
- Для MCP Node Terminal требуется:
  - сохранить общие принципы (скрипты → тесты/команды/CLI, отсутствие временных скриптов в `scripts/` и корне);
  - но привести стек и примеры в соответствие с текущим проектом (Node, CJS/ESM, PowerShell, MCP протокол, структура `tests/` и возможные пакетные тесты).

## Технические ограничения
- Нельзя нарушать уже действующее правило `.cursor/rules/tests-conversion.mdc`; обновлённое правило должно быть надстройкой/обобщением, а не противоречить ему.
- Нельзя вводить жёсткие привязки к технологиям, которых нет в этом репозитории (Laravel, PHPUnit, Playwright) как к обязательной части пайплайна.
- Все изменения должны быть текстовыми (обновление `.cursor/rules/scripts-and-tests-conversion.mdc`), без добавления/удаления тестов или скриптов в коде на этом шаге.
- Правило не должно требовать немедленного рефакторинга всего репозитория, а описывать целевое состояние и подход для новых/изменяемых скриптов.

## Ожидаемый результат
- Обновлённый `.cursor/rules/scripts-and-tests-conversion.mdc`, описывающий:
  - как в MCP Node Terminal трактуются временные скрипты и сценарии проверок;
  - куда именно их нужно мигрировать (структура `tests/`, возможные пакетные тесты, CLI-утилиты);
  - как правило соотносится с `.cursor/rules/tests-conversion.mdc` и фазой 07-testing.

## Решение
- [x] Прочитать текущую версию `.cursor/rules/scripts-and-tests-conversion.mdc` и `.cursor/rules/tests-conversion.mdc`.
- [x] Выделить части правила, которые:
  - [x] универсальны и могут быть оставлены как есть (принцип “все проверки → тесты”, судьба временных скриптов),
  - [x] специфичны для PHP/Laravel/Vitest/Playwright и требуют адаптации под MCP Node Terminal.
- [x] Переписать/адаптировать проблемные части:
  - [x] убрать прямые ссылки на PHPUnit/Laravel там, где они не применимы к этому репо;
  - [x] оставить или переформулировать Vitest/Playwright только как опциональные/примерные технологии, а не как обязательные для этого проекта;
  - [x] уточнить разделы про `tests/`, `scripts/`, корневые скрипты и пакеты применительно к Node/MCP.
- [x] Убедиться, что итоговый текст не дублирует, а дополняет `.cursor/rules/tests-conversion.mdc` (общее правило vs. специализированное для MCP Terminal).

## Результат
- [x] Обновлён файл `.cursor/rules/scripts-and-tests-conversion.mdc` с учётом специфики MCP Node Terminal.

## Статус
- Завершено

## Связанные файлы
- `.cursor/rules/scripts-and-tests-conversion.mdc` - обновлён
- `.cursor/rules/tests-conversion.mdc` - использован как база для адаптации (без изменений)


