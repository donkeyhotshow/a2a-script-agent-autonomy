# добиваем qtu json модуль

v1 функционал автоматически переходит в v5, перекрывать ненужно.
можно разделить код на блоки, чтоб можно было отключать разные части модуля.

нужно продолжить/выполнить концептуальное улучшение. сделай аналог моего сценария но со своими промтами, хочу провести
експеремент оба варианта, твой и мой, посмотрю разницу. каждый ключ массива, в файле files-by-block, ето отдельный блок,
который можно выбрать для установки, в installercontroller.php и его хелперах. переписать етот файл в v5 и тогда можно
будет убрать лишние файлы из v5 , которые копируются в предыдущих версиях. `php artisan module:merge question-to-user`
очень четко показывает дубликаты. если файл есть в младшей(номер меньше) версии, то удаляем файл в старшей(больше номер)
версии

сделать новые страници:

1. (уже есть)(переработать?) типа страница с вопросами(в v1?).
2. страница диалогов. замена варианта диалога через чат, потому что нужно чтоб эти диалоги корректировались, при
   потребности, напрямую ИИ, например, если обнаружилось недопонимание.
3. страница управления/локировки/завершения задач и сченариев, а значит должно быть передача команд в консоль и
   выполнение команд мейн или напрямую.
4. страница ..... еще бы ....
5. идея хорошая пришла. отдельная страница соспецифическими диалогами, но только в этих ИИ пассивно доправшивает
   человека, анализарует его ответы и работает над возможными скрытимы мотивами/будущимиПроблемами. главной целью будет
   возможности к росту систем и механизмов во благо развязки проблем. думаю это будет хорошим толчком для развития.
   люблю когда ИИ мой бред анализирует и ищет там скритый контекст, а для него что код программы читать. 
