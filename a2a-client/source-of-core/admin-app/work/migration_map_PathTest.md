# Migration Map for tests/Feature/AiRudeDepot/Support/PathTest.php

Эта карта сопоставляет Feature-тесты из `PathTest.php` с соответствующими классами и методами в приложении, связанными с хелперами и менеджерами для работы с путями и адресами хранилищ.

## Обзор
Тесты сосредоточены на проверке корректности парсинга различных форматов адресов хранилищ (`file!`, `directory!`, `buffer:`, т.д.) и построения объектов путей.

---

Test Method: `pathTestCases` DataProvider
- Функциональность: Предоставляет набор корректных тестовых случаев адресов хранилищ и их ожидаемого разобранного формата.
- Потенциальная зона кода: Не тестирует код приложения напрямую, является вспомогательным методом для тестов парсинга.

---

Test Method: `invalidPathTestCases` DataProvider
- Функциональность: Предоставляет набор некорректных тестовых случаев адресов хранилищ для проверки обработки ошибок.
- Потенциальная зона кода: Не тестирует код приложения напрямую, является вспомогательным методом для тестов парсинга ошибок.

---

Test Method: `testBuildPath`
- Функциональность: Тестирование хелпера `PathHelper::buildPath`, который, вероятно, используется для построения стандартизированных объектов путей на основе разобранных адресов.
- Потенциальная зона кода:
  - `App\\Helpers\\PathHelper::buildPath`
  - Структура возвращаемого объекта пути (его свойства: `storagePath`, `itemType`, `label`, `keyPathList`).

---

Test Method: `testBuildSimpleFilePath`, `testBuildFilePathWithKeys`, `testBuildDirectoryWithSpecialCharacters`
- Функциональность: Изолированные тесты для конкретных случаев `testBuildPath`, покрывающие простые файловые пути, пути с ключами и пути директорий со спецсимволами.
- Потенциальная зона кода:
  - Та же, что и `testBuildPath`, но фокусируется на специфических входных данных и их ожидаемом выводе.

---

Test Method: `testInvalidPrefixPath`, `testMissingTypePath`, `testUnsupportedItemTypePath`
- Функциональность: Тестирование обработки некорректных форматов адресов хранилищ (`invalid_prefix!`, `!system/output`, `unknown!`), проверка, что `StoragePathParser::parse` выбрасывает ожидаемое исключение `InvalidArgumentException`.
- Потенциальная зона кода:
  - `App\\AiRudeDepot\\Managers\\StoragePathParser::parse` (логика валидации префикса и типа элемента)
  - Обработка ошибок и выброс исключений в `StoragePathParser::parse`.

---

