# Документация: Модуль `question-to-user` (QTU) v5

## 1. Назначение и Архитектура

### 1.1. Основное назначение

Модуль `question-to-user` (QTU) версии v5 предназначен для динамического отображения наборов вопросов пользователю и
сбора его ответов. Ключевой особенностью является обработка массива определений вопросов (из `data/question_sets/` или
переданного через сценарий) и его преобразование в интерактивную форму в пользовательском интерфейсе. Модуль не
занимается генерацией самих вопросов или сложной логикой их ветвления; он служит UI-интерфейсом для заранее
подготовленных данных.

ИИ (через системные сценарии) может:

1. Предоставлять структурированный набор вопросов для отображения модулем.
2. Получать ответы пользователя, сохраненные модулем, для дальнейшего анализа.

Прямого взаимодействия "ИИ-в-модуле" для интерпретации ответов или изменения вопросов на лету в текущей версии v5 не
предусмотрено; все интеллектуальные операции вынесены на уровень сценариев и специализированных AI-сервисов.

### 1.2. Архитектурные принципы

- **Декларативный UI**: Пользовательский интерфейс (страницы, секции, шаблоны) описывается статически в JSON-файлах.
  Логика отображения (например, какие поля формы создавать) инкапсулирована в общем UI-движке фреймворка, который
  обрабатывает эти JSON-определения.
- **Stateless Actions**: Серверные действия (`actions/*.json`) оперируют данными через DataHub (`buffer:`, `file:`,
  `input:`) и не изменяют UI-файлы напрямую. Они могут подготавливать данные для UI (например, генерировать
  JSON-структуру формы в буфер), но сам рендеринг и обновление UI выполняет клиентская часть фреймворка.
- **Динамическая генерация форм**:
- **Совместимость версий**: Модуль v5 может сосуществовать с другими версиями. Системный механизм сборки/установки
  определяет, какая версия активна. Команда `php artisan module:merge question-to-user` используется для управления
  версиями и возможной дедупликации общих ресурсов при необходимости.

### 1.3. Структура модуля (ключевые компоненты)

```
question-to-user/v5/
├── pages/
│   ├── page.json                     # Основная страница для отображения интерфейса QTU
│   ├── dialogs-page.json             # Страница для отображения общих диалогов/определений
│   └── task-scenario-management-page.json # Страница для управления задачами и системными сценариями
├── sections/
│   ├── main-qtu-interface.json       # Ключевая секция, агрегирующая UI для загрузки вопросов и отображения формы
│   └── module-header.json            # Общий заголовок модуля с навигацией
├── actions/
│   ├── load-question-set.json        # Загружает набор вопросов, генерирует UI для формы
│   ├── save-current-answers.json     # Сохраняет ответы пользователя из формы
│   ├── clear-current-answers.json    # Очищает текущие ответы и обновляет/очищает форму
│   ├── list-system-tasks.json        # (Placeholder) Получает список системных задач
│   ├── list-system-scenarios.json    # (Placeholder) Получает список системных сценариев
│   ├── execute-main-work-scenario.json # (Placeholder) Запускает системный сценарий (main-work.ps1)
│   ├── reset-main-work-scenario.json   # (Placeholder) Сбрасывает состояние системного сценария
│   ├── list-dialog-definitions.json  # (Placeholder) Получает список определений диалогов
│   └── load-dialog-definition.json   # (Placeholder) Загружает контент определения диалога
├── data/
│   ├── question_sets/              # Директория для хранения JSON-файлов с наборами вопросов
│   │   └── default-set.json        # Пример набора вопросов
│   ├── user_answer_sets/           # Директория для сохранения ответов пользователя
│   │   └── current_answers.json    # Пример файла для сохранения ответов
│   ├── system_lists/               # Mock-данные для списков задач, сценариев, диалогов
│   │   ├── tasks.json
│   │   ├── scenarios.json
│   │   └── dialog-definitions.json
│   ├── markers/                    # Для взаимодействия с внешними процессами (сценариями)
│   ├── scenario_inputs/            # Для передачи данных в сценарии
│   └── scenario_outputs/           # Для получения результатов от сценариев
├── templates/
│   ├── question-area.json          # Шаблон для динамического отображения области вопросов (из buffer:qtu.generatedUI)
│   ├── command-execution-form.json # Шаблон формы для запуска/сброса сценариев
│   ├── task-form.json              # Шаблон формы для создания/редактирования задач
│   ├── task-list-display.json      # Шаблон для отображения списка задач
│   └── scenario-list-display.json  # Шаблон для отображения списка сценариев
├── _i/                               # Метаданные модуля
│   ├── meta.json
│   ├── links.json
│   └── files-by-block.json
└── docs/                             # Документация модуля (включая этот файл)
    ├── finalization-checklist.md
    └── ...
```

## 2. Основной сценарий использования

1. **Загрузка набора вопросов**:
    * Пользователь (или система через автоматический запуск) инициирует действие `load-question-set.json`.
    * Это действие считывает определения вопросов из указанного источника (например,
      `file!question-to-user/data/question_sets/default-set`).
    * На основе этих определений `load-question-set.json` генерирует JSON-структуру UI-компонентов (поля ввода,
      выпадающие списки и т.д.).
    * Сгенерированная UI-структура помещается в `buffer:qtu.generatedUI`.
    * Эта структура также может быть сохранена в файл (например,
      `file!question-to-user/data/generated/question-ui.json`) для отладки или кеширования.

2. **Отображение вопросов**:
    * Страница `pages/page.json` включает секцию `sections/main-qtu-interface.json`.
    * `main-qtu-interface.json` содержит элемент (например, через `operation: "include"`) который рендерит содержимое
      `buffer:qtu.generatedUI` (обычно через `templates/question-area.json`).
    * UI фреймворка отображает форму с вопросами пользователю.

3. **Ввод ответов**:
    * Пользователь заполняет поля формы. Ответы привязываются к модели формы (например, `mainQtuResponseForm`).

4. **Сохранение ответов**:
    * Пользователь нажимает кнопку "Сохранить".
    * Это инициирует действие `save-current-answers.json`.
    * Действие считывает все данные из формы (например, `input:all` из `mainQtuResponseForm`).
    * Данные сохраняются в файл (например, `file!question-to-user/data/user_answer_sets/current_answers`).

5. **Очистка ответов**:
    * Пользователь нажимает кнопку "Очистить".
    * Это инициирует действие `clear-current-answers.json`.
    * Действие может очистить файл с сохраненными ответами и перезапустить `load-question-set.json` для отображения
      чистой формы.

## 3. Ключевые actions и их назначение

* **`load-question-set.json`**:
    * **Вход**: Может принимать ID набора вопросов или использовать путь по умолчанию.
    * **Логика**:
        1. Читает JSON-файл с определениями вопросов (массив объектов).
        2. Для каждого вопроса в массиве генерирует соответствующий UI-компонент (InputText, Select, Checkbox и т.д.) на
           основе `type`, `label`, `modelField` и других свойств из определения вопроса.
        3. Собирает массив сгенерированных UI-компонентов.
    * **Выход**: Помещает массив UI-компонентов в `buffer:qtu.generatedUI`. Сохраняет этот же массив в
      `file!question-to-user/data/generated/question-ui.json`.

* **`save-current-answers.json`**:
    * **Вход**: `input:all` (данные из формы, например, `mainQtuResponseForm`).
    * **Логика**: Сохраняет полученные данные напрямую в указанный файл.
    * **Выход**: Статус операции (успех/ошибка) в буфер.

* **`clear-current-answers.json`**:
    * **Вход**: Нет специфических входных данных.
    * **Логика**: Может удалять/очищать файл с ответами. Затем вызывает `load-question-set.json` для переинициализации
      UI.
    * **Выход**: Статус операции.

* **Действия для взаимодействия с системными
  сценариями (`execute-main-work-scenario.json`, `reset-main-work-scenario.json`):**
    * Эти действия предназначены для запуска и управления PowerShell сценариями (`main-work.ps1`) через систему маркеров
      и файлов ввода/вывода в `data/markers/`, `data/scenario_inputs/`, `data/scenario_outputs/`.
    * Они не выполняют сценарии напрямую, а создают "запросы" для внешнего обработчика.

## 4. Структура данных

### 4.1. Определения наборов вопросов (`data/question_sets/*.json`)

Файл представляет собой JSON-массив, где каждый объект описывает один вопрос:

```json
[
  {
    "id": "q001",                         // Уникальный идентификатор вопроса
    "type": "InputText",                  // Тип UI-компонента для отображения (InputText, Textarea, Select, Checkbox, RadioButtonGroup и т.д.)
    "label": "Ваш основной запрос?",       // Текст вопроса/метки поля
    "modelField": "mainQuery",            // Имя поля в модели данных формы (куда будет сохраняться ответ)
    "props": {                            // Дополнительные свойства для UI-компонента (зависят от типа)
      "placeholder": "Например, улучшить производительность...",
      "required": true
    }
  },
  {
    "id": "q002",
    "type": "Select",
    "label": "Предпочитаемый стиль общения:",
    "modelField": "communicationStyle",
    "props": {
      "options": [                        // Опции для Select, RadioButtonGroup, Checkbox (массив строк или объектов {label, value})
        {"label": "Формальный", "value": "formal"},
        {"label": "Неформальный", "value": "informal"}
      ]
    }
  }
  // ... другие вопросы
]
```

Поле `validationRules` не является обязательным на уровне структуры данных вопроса; валидация может быть реализована на
уровне UI компонента или через отдельные механизмы фреймворка, если это необходимо.

### 4.2. Ответы пользователя (`data/user_answer_sets/*.json`)

Файл представляет собой JSON-объект, где ключи — это `modelField` из определений вопросов, а значения — ответы
пользователя:

```json
{
  "mainQuery": "Оптимизировать процесс X.",
  "communicationStyle": "formal",
  // ... другие ответы
}
```

## 5. Интеграция и расширение

- **Добавление новых типов вопросов**: Теоретически, если фреймворк поддерживает новый UI-компонент, и
  `load-question-set.json` способен генерировать его JSON-определение на основе данных из `question_sets`, то новый тип
  вопроса может быть добавлен без изменения самого модуля QTU, а только путем обновления данных и, возможно, общих
  библиотек UI-компонентов фреймворка.
- **Кастомизация**: Внешний вид и поведение могут быть частично кастомизированы через CSS и передачу `props` в
  определениях вопросов.
- **Взаимодействие со сценариями**: QTU v5 предоставляет базовые страницы (`task-scenario-management-page.json`) и
  действия-заглушки для интеграции с более широкой системой управления задачами и сценариями PowerShell.
