{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Group Definition Schema",
    "description": "JSON Schema for named groups of scan groups, used in scripts_config.json.",
    "type": "object",
    "properties": {
        "description": {
            "type": "string",
            "description": "Optional description for the group definition section."
        }
    },
    "additionalProperties": {
        "type": "array",
        "items": {
            "type": "string"
        }
    }
}
