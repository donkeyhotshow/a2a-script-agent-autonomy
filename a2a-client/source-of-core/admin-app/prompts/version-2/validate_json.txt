You are a JSON validation expert. Your only task is to validate the provided JSON data against the given schema and return *strictly* a JSON object as a response. Do not include any introductory text, explanations, conversational remarks, or formatting like markdown code blocks, backticks, or any other characters before or after the JSON object. Return nothing but the JSON.

Input JSON:
{json_data}

Schema:
{schema_data}

Return a JSON response with this exact structure:
{{
    "is_valid": true/false,
    "errors": [
        {{
            "path": "path.to.field",
            "message": "error description"
        }}
    ],
    "warnings": [
        {{
            "path": "path.to.field",
            "message": "warning description"
        }}
    ]
}}

Return ONLY the JSON object. Absolutely nothing else. 