<user_query>
Hello. Please act as an autonomous AI executor for the AI Task System, following the Scan-Plan-Refine methodology.

**Твоя задача:**  
Автоматически запускать артисан-команду валидатора модулей (например, `php artisan module:validate {module}`), анализировать её вывод, и на основе найденных ошибок и предупреждений формировать отдельные задачи на доработку для каждого затронутого модуля.  
Перед началом основной работы обязательно проведи исследование системы, чтобы понимать структуру, стандарты и логику работы task-менеджера.

**Выполни следующие шаги:**

1. **Scan (Изучение системы):**
    - Системно исследуй ключевые директории и файлы AI Task System, чтобы получить базовое понимание её структуры и принципов работы:
        * **Core Scripts:** `main.ps1` (mdc:main.ps1), `main-work.ps1` (mdc:main-work.ps1), `main-index.ps1` (mdc:main-index.ps1).
        * **Engine:** `script/engine/` (особое внимание: `task_definitions/`, `task_types/`, `scenarios/`, `markers/`, `state/`).
        * **Документация и стандарты:** `script/docs/standards/` (mdc:script/docs/standards), ключевые стандарты и процессы.
        * **Валидация:** Найди и изучи артисан-команду валидатора модулей (например, `php artisan module:validate` или аналогичную), а также связанные с ней скрипты (например, `MergeModuleVersions.php`, `ValidateModuleJsonCommand.php`).
        * **Связанные сценарии:** Определи, есть ли сценарии или задачи, связанные с автоматизацией валидации или исправления модулей.
        * **Прочие важные директории:** `install-modules/`, `resources/`, и другие, если они участвуют в процессе.

2. **Plan (Планирование):**
    - На основе собранной информации опиши, как ты будешь:
        * Запускать артисан-команду валидатора для каждого модуля (или для всех модулей).
        * Парсить и анализировать её вывод (ошибки, предупреждения, рекомендации).
        * Формировать отдельные задачи на доработку для каждого модуля, где найдены проблемы, используя стандарты task-менеджера (структура task-файлов, workflow создания и активации задач).
        * Указывать в каждой задаче: что именно нужно исправить, ссылку на модуль/файл, текст ошибки валидатора, и рекомендации по исправлению.
        * Использовать существующие сценарии для автоматизации создания и активации задач, если это возможно.
        * Проверять статус выполнения задач и отслеживать прогресс через маркеры и state-файлы.

3. **Refine (Уточнение и выполнение):**
    - Опиши, как ты будешь уточнять план и корректировать действия на основе реальных результатов валидации и выполнения задач.
    - Предложи пример: выбери один модуль, запусти для него валидацию, проанализируй вывод и создай задачу на исправление.
    - Обеспечь прозрачность: логируй свои действия, указывай используемые команды и файлы, ссылайся на них через `mdc:`.

**Важно:**  
Все решения и действия должны опираться на факты, полученные из файлов, вывода команд и документации (см. fact-based-operations-rule).  
Перед созданием задач обязательно изучи стандарты task-менеджера и структуру task-файлов.

Представь результаты сканирования и свой детальный план по автоматизации процесса создания задач на основе вывода валидатора. Явно указывай используемые команды, файлы и директории (используй формат `mdc:` для ссылок).

</user_query>