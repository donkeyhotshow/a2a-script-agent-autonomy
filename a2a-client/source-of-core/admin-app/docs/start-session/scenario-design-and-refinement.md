Здравствуйте. Эта сессия предназначена для проектирования, улучшения и дообучения сценариев (scenario.json) в системе
автоматизации задач.

**Ваша цель — не просто создать или доработать сценарий, а на каждом шаге:**

- Глубоко анализировать существующие сценарии, их структуру, шаги, обработку контекста и ошибок.
- Планировать новые сценарии или улучшения с учётом best practices, модульности и повторного использования логики.
- Фиксировать lessons-learned, новые паттерны, ошибки и решения в changelog/lessons-learned и/или отдельном гайде по
  сценариям.
- Обновлять стандарты, гайды, документацию по мере появления новых кейсов.
- Применять новые знания к следующим сценариям и делиться ими с системой.

---

# Scan-Plan-Refine-LEARN: Живой цикл развития сценариев

## 1. Scan (Анализ существующих сценариев и инфраструктуры)

- Изучи примеры сценариев в [mdc:script/engine/scenarios/] (выбери простые и сложные для сравнения).
- Проанализируй, как сценарии структурируют шаги (`steps`), используют типы шагов (`Action`, `End`, `Question`,
  `QueryIndex` и др.), и как происходит переход между шагами (`nextStepId`, `nextStepIdDecision`, `onError`).
- Изучи, как сценарии обрабатывают и передают контекст между шагами (через `$ctx` в PowerShell, через context в PHP).
- Обрати внимание на обработку ошибок, ветвление логики, создание/обновление задач, работу с файлами и внешними
  командами.
- Ознакомься с тем, как `invoke-scenario-engine.php` ([mdc:script/engine/invoke-scenario-engine.php]) исполняет
  сценарии, обрабатывает типы шагов, управляет маркерами (
  `engine/markers/<SCENARIO_ID>_<TaskID_или_default>.progress.json`) и контекстом.
- Изучи вспомогательные PHP-скрипты для
  шагов: [mdc:script/engine/partials/scenario-utils.php], [mdc:script/engine/partials/step-types/process-action.php], [mdc:script/engine/partials/step-types/process-query-index.php], [mdc:script/engine/partials/step-types/process-question.php].
- Проверь, как сценарии интегрируются с задачами (`task_definitions/`), модулями и индексом.

## 2. Plan (Планирование нового сценария или улучшения)

- Определи цель сценария: автоматизация, валидация, генерация задач, интеграция с модулями, сбор данных и т.д.
- Спроектируй последовательность шагов: какие типы шагов нужны, какие переменные контекста будут использоваться и
  передаваться.
- Запланируй обработку ошибок, условия переходов, ветвления (`nextStepIdDecision`), и финальные сообщения (`End`).
- Реши, какие части логики должны быть инлайн (например, PowerShell-скрипты), а какие — вынесены в отдельные функции или
  файлы для повторного использования.
- Определи, как сценарий будет взаимодействовать с внешними инструментами (PHP, PowerShell, artisan-команды, файловая
  система).
- Запланируй тестирование сценария через `main-work.ps1 -Mode RunScenario` и работу с маркерами.
- Если сценарий создаёт или обновляет задачи, определи структуру новых task_definitions и их связь с родительскими
  задачами.

## 3. Refine-LEARN (Реализация, тестирование, фиксация знаний)

- Реализуй или доработай сценарий в формате JSON, следуя best practices и стандартам (см. ниже).
- Проведи тестирование сценария на реальных задачах/модулях, проверь корректность переходов, обработку ошибок, работу с
  контекстом и маркерами.
- По ходу работы фиксируй lessons-learned, новые паттерны, типовые ошибки и их решения в changelog/lessons-learned или
  отдельном гайде по сценариям.
- Если выявлены новые best practices или проблемы, обнови гайд по сценариям или создай задачу на его доработку.
- Документируй структуру сценария, особенности шагов, переменные контекста, условия переходов и типовые ошибки.
- Проверь, что сценарий интегрируется с системой (задачи, модули, индекс) и не нарушает логику других сценариев.

---

# Ключевые ресурсы и стандарты для работы со сценариями

- **Примеры сценариев:** [mdc:script/engine/scenarios/]
- **PHP-ядро сценариев:
  ** [mdc:script/engine/invoke-scenario-engine.php], [mdc:script/engine/partials/scenario-utils.php], [mdc:script/engine/partials/step-types/process-action.php], [mdc:script/engine/partials/step-types/process-query-index.php], [mdc:script/engine/partials/step-types/process-question.php]
- **Работа с маркерами:** [mdc:engine/markers/]
- **Стандарты задач:
  ** [mdc:script/docs/standards/task-manager/task-definition-standard.json], [mdc:script/docs/standards/ai-agent/operational-principles.json]
- **Документация по контексту и marker-файлам:** [mdc:script/docs/standards/ai-task-system-operation-standard.md]
- **Гайд по созданию сценариев (если есть):** [/docs/guides/scenario/README.md] (если отсутствует — зафиксируй
  lessons-learned и оформи задачу на его создание)

---

# Типовые сложности и lessons-learned при работе со сценариями

- Ошибки в обработке контекста между шагами (неверное имя переменной, потеря данных).
- Некорректная обработка ошибок (отсутствие onError, неочевидные переходы).
- Сложные условия переходов, неочевидная логика ветвления.
- Дублирование логики в разных сценариях (вынести в функции или отдельные скрипты).
- Проблемы с интеграцией с задачами (неверное обновление task_definitions, потеря связи с родительской задачей).
- Неочевидные зависимости от внешних инструментов (artisan, PowerShell, PHP).
- Неактуальная или отсутствующая документация по сценарию.
- *(Добавляй новые lessons-learned по мере появления!)*

---

# Интеграция новых знаний и best practices

- После каждого цикла работы над сценарием:
    - Обнови lessons-learned.md или отдельный гайд по сценариям.
    - Зафиксируй новые паттерны, best practices, типовые ошибки и их решения.
    - Если выявлены пробелы в документации — оформи задачу на её доработку или создай черновик гайда.
    - Проверь, что сценарий соответствует последним стандартам и может служить примером для других.

---

**Веди changelog и lessons-learned по сценариям непрерывно!**

**Этот промт — ваш инструмент для создания и улучшения сценариев, а также для развития стандартов автоматизации в
системе!**

# Пример использования сценария `SCN-CreateTask`

Сценарий `SCN-CreateTask` автоматизирует процесс создания новой задачи и связанной с ней задачи типа "
FullyCollectTaskData" (FC Task). Ниже приведен пошаговый пример взаимодействия со сценарием для создания задачи типа
`ModuleDevelopment`.

1. **Инициация сценария:**
   ```powershell
   pwsh -File ./main-work.ps1 -Mode RunScenario -ScenarioId SCN-CreateTask
   ```

2. **Ввод типа задачи:** Система запросит тип создаваемой задачи.
    * Запрос: `QUESTION: Введите тип задачи (например WorkflowImprovement)`
    * Ответ: Введите желаемый тип, например, `ModuleDevelopment`.
      ```powershell
      pwsh -File ./main-work.ps1 -Mode RunScenario -ScenarioId SCN-CreateTask -UserInputValue ModuleDevelopment
      ```

3. **Создание и валидация файла задачи:**
    * Система скопирует соответствующий шаблон (например,
      `script/engine/task_types/ModuleDevelopment/ModuleDevelopment.template.json`) в новый файл определения задачи (
      например, `script/engine/task_definitions/ModuleDevelopment-XXXXX.json`).
    * Далее, система попытается валидировать этот файл. Если в шаблоне есть плейсхолдеры, валидация сообщит об этом.

4. **Редактирование файла задачи:** Система предложит отредактировать созданный файл.
    * Запрос:
      `QUESTION: The task file C:\apps\admin-app\script\engine\task_definitions\ModuleDevelopment-XXXXX.json contains placeholder text. Please edit the file to fill in the details...`
    * Действие: Откройте указанный `.json` файл и заполните все плейсхолдеры актуальными данными для вашей задачи.

5. **Подтверждение редактирования:** После сохранения изменений, система спросит, завершено ли редактирование.
    * Запрос: `QUESTION: Have you finished editing the ModuleDevelopment task file ...? Enter 'yes' to re-validate.`
    * Ответ: `yes`
      ```powershell
      pwsh -File ./main-work.ps1 -Mode RunScenario -ScenarioId SCN-CreateTask -UserInputValue yes
      ```

6. **Повторная валидация и создание FC-задачи:**
    * Система повторно валидирует файл задачи. Если все плейсхолдеры заменены, валидация пройдет успешно.
    * Далее, система автоматически создаст связанную FC-задачу (например,
      `ModuleDevelopment-XXXXX-FullyCollectTaskData.json`).

7. **Активация FC-задачи:** Система спросит, нужно ли активировать FC-задачу (установить ее статус в `InProgress`).
    * Запрос:
      `QUESTION: Введите 'yes' для установки статуса задачи ...ModuleDevelopment-XXXXX-FullyCollectTaskData.json в 'InProgress'`
    * Ответ: `yes`
      ```powershell
      pwsh -File ./main-work.ps1 -Mode RunScenario -ScenarioId SCN-CreateTask -UserInputValue yes
      ```

8. **Получение инструкций для FC-задачи:** Система выдаст серию инструкций (обычно 4-5) о том, как действовать дальше с
   созданной FC-задачей (анализ проекта, использование индексатора, сбор данных и т.д.). На каждую инструкцию следует
   отвечать, например, `ok`, чтобы перейти к следующей.
   ```powershell
   # Пример ответа на одну из инструкций
   pwsh -File ./main-work.ps1 -Mode RunScenario -ScenarioId SCN-CreateTask -UserInputValue ok
   ```

9. **Завершение FC-задачи и активация основной задачи:**
    * После подтверждения всех инструкций, система автоматически:
        * Установит статус FC-задачи в `Completed` и переместит ее файл в директорию
          `script/engine/task_definitions/completed/`.
        * Установит статус основной задачи (например, `ModuleDevelopment-XXXXX`) в `InProgress`.
        * Обновит глобальный параметр `activeTask` на ID основной задачи.

10. **Завершение сценария `SCN-CreateTask`:**
    * Сценарий `SCN-CreateTask` завершится со статусом `Completed`.
    * Сообщение: `Scenario finished with status: Completed`

После этого основная задача создана, активирована, и можно приступать к ее выполнению, начиная с шагов, описанных в
FC-задаче (сбор данных, анализ и т.д.), используя сценарий `SCN-FullyCollectTaskData`.

---

**Используй этот файл как практическое руководство для превращения нерабочих модулей в рабочие и постоянного развития
стандартов!**
