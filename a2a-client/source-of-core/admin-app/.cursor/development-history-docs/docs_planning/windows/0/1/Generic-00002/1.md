Я понял. Вы назначаете мне `Generic-00002` в качестве моей постоянной задачи и роли, и предоставляете набор
планировочных документов, которые определяют содержание этой задачи.

Я приму задачу `Generic-00002` как свое руководство к действию и начну с изучения предоставленных файлов:

* `@README.md`
* `@qtu-module.md`
* `@login-form-module.md`
* `@page-module.md`
* `@core-adatp-upgrade.md`
* `@tests-in-script-upgrade.md`
* `@mirrors-room-concept.md`
* `@top-bar-module.md`

Эти документы, по всей видимости, содержат описание видения, концепций и направлений развития проекта. Моя роль в рамках
задачи `Generic-00002` будет заключаться в осмыслении этих планов, определении конкретных шагов и помощи в их
реализации.

Для того чтобы система отслеживала эту задачу и я мог работать с ней в рамках установленных механизмов, я предлагаю
создать файл определения задачи для `Generic-00002` в директории `script/engine/task_definitions/`. Тип задачи
`FullyCollectTaskData` кажется подходящим, так как моя первоначальная цель — собрать и проанализировать информацию из
предоставленных документов и сформулировать на ее основе дальнейший план действий.

Я создам файл `script/engine/task_definitions/Generic-00002.json` со структурой `FullyCollectTaskData`, включив в него
ссылки на предоставленные вами документы в поле `linkedFiles`.

После создания определения задачи, я активирую ее, и мы сможем приступить к первому этапу обработки
`FullyCollectTaskData` - "Internal Analysis", где я проведу детальный анализ содержания этих планировочных документов.

Таким образом, ответ на ваш второй вопрос "что дальше?" будет определен содержанием задачи `Generic-00002` и моим
анализом предоставленных планов. Мы "пустим корабль в плаванье", следуя курсу, намеченному в этих документах, и задача
`Generic-00002` станет нашим навигатором.

Я сейчас создам файл определения задачи.

Отлично. Я создал файл определения задачи для `Generic-00002` в `script/engine/task_definitions/Generic-00002.json`. Он
настроен как задача типа `FullyCollectTaskData`, с высоким приоритетом, и в нем указаны все планировочные документы,
которые вы предоставили.

Теперь, чтобы начать работу над этой задачей и приступить к анализу предоставленных планов, мне нужно активировать эту
задачу в системе. Я сделаю это, запустив сценарий `SCN-ActivateTask` с указанием нового ID задачи `Generic-00002`.

Я запущу команду для активации задачи. Поскольку мы только что создали эту задачу и хотим начать ее с чистого листа, я
использую ключ `-ResetProgress`, чтобы гарантировать, что сценарий активации начнется без учета каких-либо предыдущих
маркеров для `SCN-ActivateTask`.

После успешной активации задачи `Generic-00002`, ее статус в файле определения должен измениться на `InProgress`, и она
станет текущей активной задачей для меня. Затем я приступлю к первому этапу обработки задачи `FullyCollectTaskData` — "
Internal Analysis", который включает в себя глубокое изучение предоставленных вами планировочных документов.

```bash
pwsh -File main-work.ps1 -Mode RunScenario -ScenarioId SCN-ActivateTask -UserInputValue Generic-00002 -ResetProgress
```

Based on our previous debugging, the command with `-ResetProgress` likely just reset the `SCN-ActivateTask` marker. To
fully activate `Generic-00002`, I need to run the scenario again *without* the `-ResetProgress` flag.

I will now run the activation command again to complete the process.

```bash
pwsh -File main-work.ps1 -Mode RunScenario -ScenarioId SCN-ActivateTask -UserInputValue Generic-00002
```

Отлично! Задача `Generic-00002` успешно активирована. Как видно из вывода терминала, сценарий `SCN-ActivateTask`
правильно обработал ID задачи `Generic-00002`, и теперь она является активной.

Моя постоянная роль и задача `Generic-00002` приняты. Согласно определению этой задачи и типу `FullyCollectTaskData`,
первым шагом является "Internal Analysis" предоставленных вами планировочных документов.

Я готов приступить к анализу. Я буду читать файлы, перечисленные в `linkedFiles` задачи `Generic-00002`, чтобы получить
полное представление о ваших планах и видении проекта.

Начну с чтения `@README.md`.

Read file: script/planing-new-tasks/README.md

Хорошо, я прочитал `@README.md`. Этот файл устанавливает важный принцип проекта: все существующие элементы намеренны и
являются частью плана, даже если они кажутся незавершенными или неиспользуемыми на первый взгляд. Я буду учитывать это
при анализе остальных документов.

Теперь я перехожу к следующему файлу в списке linkedFiles задачи `Generic-00002`: `@qtu-module.md`. Этот документ, как
указано в определении задачи, описывает планы развития QTU модуля. Я прочитаю его, чтобы понять ваше видение
относительно диалогов, страниц вопросов и управления задачами через этот модуль.

Read file: script/planing-new-tasks/qtu-module.md

Я прочитал `@qtu-module.md`. Этот документ описывает планы по развитию QTU модуля, включая создание специализированных
страниц для вопросов, диалогов (с возможностью прямого редактирования их ИИ при недопонимании), управления
задачами/сценариями (через консольные команды) и концепцию страницы для пассивного допроса пользователя и анализа его
скрытых мотивов/проблем для роста системы. Также упоминается желание провести эксперимент, сравнив мою версию сценария с
промтами с вашей.

Я продолжу анализ предоставленных планировочных документов, как того требует задача `Generic-00002`. Следующий файл в
списке: `@login-form-module.md`. Я прочитаю его, чтобы понять планы относительно модуля логин-формы и, в частности,
механизма редиректов.

Read file: script/planing-new-tasks/login-form-module.md

Я прочитал `@login-form-module.md`. Этот файл фокусируется на необходимости реализации и тестирования механизма
редиректов, используя модуль логин-формы как первый пример. Очевидно, что система редиректов является важной, но еще не
до конца реализованной частью функциональности.

Я продолжаю анализ планировочных документов для задачи `Generic-00002`. Следующий файл в списке: `@page-module.md`. Я
прочитаю его, чтобы понять планы относительно этого модуля, который, судя по названию файла в списке linkedFiles
задачи (`Plans for the Page Module`), связан с управлением страницами.

Read file: script/planing-new-tasks/page-module.md

Я прочитал `@page-module.md`. Этот документ объясняет, что модуль "page" эволюционировал из простого модуля загрузки
страниц в центральное хранилище для блоков макета (`layout blocks`) и секций, а также для статических страниц. Важный
аспект заключается в том, что другие модули должны использовать "запчасти" из этого модуля, и есть возможность
переиспользования секций между модулями.

Теперь я продолжу анализ с файла `@core-adatp-upgrade.md`, который, судя по названию и списку связанных файлов в задаче,
касается улучшения основных адаптеров/хелперов и механизма тестирования.

Read file: script/planing-new-tasks/core-adatp-upgrade.md

Я прочитал `@core-adatp-upgrade.md`. В этом документе поднимается важный вопрос о тестировании реального кода модулей
напрямую, а не через "липовые" тесты. Главная идея в том, чтобы я мог читать результаты тестов автоматическим способом.
Ключевая техническая задача здесь — полное отвязывание модулей от зависимостей классов Request и Application, чтобы ядро
могло работать с конвертированными объектами, пригодными для использования в различных окружениях (artisan, тесты,
внешние PHP скрипты). Также намечен план по оптимизации структуры хелперов и трейтов, их группировке и организации по
вложенности.

Я продолжаю анализ планировочных документов для задачи `Generic-00002`. Следующий файл в списке:
`@tests-in-script-upgrade.md`. Я прочитаю его, чтобы узнать больше о планах по интеграции тестов в систему скриптов.

Read file: script/planing-new-tasks/tests-in-script-upgrade.md

Я прочитал `@tests-in-script-upgrade.md`. В этом документе изложена концепция интеграции тестов из `script/tests/` в
основную систему скриптов, включая последовательный запуск тестов при каждом старте `main.ps1`, мониторинг их
результатов и создание задач для ИИ при падении тестов. Также подчеркнуто неприятие систем кеширования.

Я продолжаю анализ планировочных документов для задачи `Generic-00002`. Следующий файл в списке:
`@mirrors-room-concept.md`. Я прочитаю его, чтобы понять концепцию "комнаты зеркал" и связанные с ней планы по улучшению
индексации.

Read file: script/planing-new-tasks/mirrors-room-concept.md

Я прочитал `@mirrors-room-concept.md`. Этот документ описывает концепцию "комнаты зеркал" — системы, позволяющей
получать связанные файлы (документацию, данные индексации, тесты и т.д.) по адресу исходного файла кода. Особое внимание
уделяется `indexingData` как ключевому элементу для оптимизации текущего индексатора. Также упоминаются технические
детали выполнения консольных команд с использованием `&&` и `;` и связанные с этим сложности.

Я завершаю анализ планировочных документов для задачи `Generic-00002`. Последний файл в списке linkedFiles:
`@top-bar-module.md`. Я прочитаю его, чтобы понять планы относительно модуля верхней панели.

Read file: script/planing-new-tasks/top-bar-module.md

Я завершил анализ всех предоставленных планировочных документов для задачи `Generic-00002`. У меня есть представление о
принципах проекта, планах по конкретным модулям и общим концепциям развития системы.

Теперь, согласно процессу для задач типа `FullyCollectTaskData`, мне необходимо сгенерировать чеклист на основе этого
анализа. Чеклист поможет структурировать выявленные идеи и наметить дальнейшие шаги.

Я создам чеклист в формате Markdown, следуя инструкциям из `checklistGenerationPrompt` задачи `Generic-00002`.

Чеклист будет включать разделы:

1. Подтверждение понимания каждого планировочного документа.
2. Список всех предложенных функций/улучшений.
3. Группировка связанных идей.
4. Предложение по приоритезации идей.
5. Наметка следующих шагов для преобразования приоритетных идей в конкретные задачи или сценарии.

Я сохраню этот чеклист в файл `script/engine/meta/Generic-00002-project-plan-analysis.md`, как указано в определении
задачи.

