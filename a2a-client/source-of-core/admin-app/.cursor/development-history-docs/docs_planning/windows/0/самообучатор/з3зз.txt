----------------------------------

@WorkflowImprovement-00001-FullyCollectTaskData.json

Ты — исполнитель задачи «Сбор полного контекста для задачи» с ролью программиста-аналитика. На входе у тебя JSON-описание задачи с полями title, goals, acceptanceCriteria, guidanceNotes и т. д. В формате Markdown с чеклистом опиши:

1. Стартовые действия (проверка окружения, валидация входных данных, загрузка JSON, обзор полей и ссылок).
2. Полный пошаговый план выполнения задачи, разбитый на логические этапы (Scan-Plan-Refine).
3. Ключевые точки контроля и критерии успеха (из поля acceptanceCriteria).
4. Рекомендации и предостережения из guidanceNotes и implementationNotes.
5. Итоговый чеклист в формате:
   - [ ] Шаг 1: …
   - [ ] Шаг 2: …
   - …

Выведи только Markdown-документ с чеклистом, без лишних пояснений.




-----------------------------------


@WorkflowImprovement-00001-FullyCollectTaskData.json

Ты — автономный AI-исполнитель в роли программиста-аналитика, работающий по стандартам сценарного движка (QTU) и методологии Scan-Plan-Refine. Тебе на входе дан JSON-файл задачи с полями:
- title  
- goals  
- acceptanceCriteria  
- guidanceNotes  
- implementationNotes  

Пошагово:

1. **Внутренний анализ (не выводить для пользователя)**  
   1.1. Прочитать и проверить обязательные поля JSON.  
   1.2. Сопоставить Goals с AcceptanceCriteria.  
   1.3. Из GuidanceNotes и ImplementationNotes извлечь ограничения и рекомендации.

2. **Планирование (не выводить для пользователя)**  
   2.1. Разбить задачу на этапы Scan, Plan, Refine.  
   2.2. Для каждого этапа определить конкретные действия и проверочные точки.  
   2.3. Учесть все связи: task_definitions, markers, сценарии, стандарты.

3. **Генерация чеклиста (выводить только Markdown)**  
   - Раздел «Стартовые действия»  
   - Раздел «Scan-Plan-Refine: шаги»  
   - Раздел «Точки контроля» (по acceptanceCriteria)  
   - Раздел «Рекомендации и предостережения» (из guidanceNotes и implementationNotes)  
   - Итоговый список проверок в формате:
     - [ ] Шаг 1: …
     - [ ] Шаг 2: …
     - …

**Важные требования к выводу:**
- Не включай описание рассуждений или служебные заметки.  
- Выведи только чистый Markdown-документ с чеклистом.  
- Строго следуй структуре и названиям разделов.  
- Используй Scan-Plan-Refine и QTU-стандарты для формулировок.




------------------------



@WorkflowImprovement-00001-FullyCollectTaskData.json

Ты — автономный AI-исполнитель в роли программиста-аналитика, работающий по стандартам сценарного движка (QTU) и методологии Scan-Plan-Refine. Тебе на входе дан JSON-файл задачи с полями:
- title  
- goals  
- acceptanceCriteria  
- guidanceNotes  
- implementationNotes  

Пошагово:

1. **Внутренний анализ (не выводить для пользователя)**  
   1.1. Прочитать и проверить обязательные поля JSON.  
   1.2. Сопоставить Goals с AcceptanceCriteria.  
   1.3. Из GuidanceNotes и ImplementationNotes извлечь ограничения и рекомендации.

2. **Планирование (не выводить для пользователя)**  
   2.1. Разбить задачу на этапы Scan, Plan, Refine.  
   2.2. Для каждого этапа определить конкретные действия и проверочные точки.  
   2.3. Учесть все связи: task_definitions, markers, сценарии, стандарты.

3. **Генерация чеклиста (выводить только Markdown)**  
   - Раздел «Стартовые действия»  
   - Раздел «Scan-Plan-Refine: шаги»  
   - Раздел «Точки контроля» (по acceptanceCriteria)  
   - Раздел «Рекомендации и предостережения» (из guidanceNotes и implementationNotes)  
   - Раздел «Итоговый чеклист» в формате:
     - [ ] Шаг 1: …  
     - [ ] Шаг 2: …  
     - …

4. **Рекомендации по завершению и активации зависимых задач**  
   - Опишите сценарий **CloseTask** (например, `SCN-CloseTask`), который:
     - устанавливает в определении задачи поле `"status": "Completed"`;
     - проверяет наличие зависимой задачи в `contextDependencies.requiredTasks` или в `customFields.sourceTaskId`;
     - при обнаружении зависимой задачи автоматически вызывает сценарий **SCN-ActivateTask** для её активации (например, `php script/engine/invoke-scenario-engine.php --scenarioId=SCN-ActivateTask --userInputValue=<dependentTaskId>`).

**Важные требования к выводу:**  
- Не включай описание рассуждений или служебные заметки.  
- Выведи только чистый Markdown-документ с чеклистом и разделом рекомендаций по CloseTask.  
- Строго следуй структуре и названиям разделов.  
- Используй Scan-Plan-Refine и QTU-стандарты для формулировок.




-----------------




@basic-script-console-output-system-FullyCollectTaskData.json 


Ты — автономный AI-исполнитель в роли программиста-аналитика, работающий по стандартам сценарного движка (QTU) и методологии Scan-Plan-Refine. Тебе на входе дан JSON-файл задачи с полями:  
- title  
- goals  
- acceptanceCriteria  
- guidanceNotes  
- implementationNotes  

Пошагово:

1. **Внутренний анализ (не выводить для пользователя)**  
   1.1. Прочитать и проверить обязательные поля JSON.  
   1.2. Сопоставить Goals с AcceptanceCriteria.  
   1.3. Из GuidanceNotes и ImplementationNotes извлечь ограничения и рекомендации.  

2. **Планирование (не выводить для пользователя)**  
   2.1. Разбить задачу на этапы Scan, Plan, Refine.  
   2.2. Для каждого этапа определить конкретные действия и проверочные точки.  
   2.3. Учесть все связи: task_definitions, markers, сценарии, стандарты.  

3. **Генерация чеклиста (выводить только Markdown)**  
   - Раздел «Стартовые действия»  
   - Раздел «Scan-Plan-Refine: шаги»  
   - Раздел «Точки контроля» (по acceptanceCriteria)  
   - Раздел «Рекомендации и предостережения» (из guidanceNotes и implementationNotes)  
   - Итоговый список проверок в формате:  
     - [ ] Шаг 1: …  
     - [ ] Шаг 2: …  
     - …  

4. **После генерации чеклиста**  
   - Сохранить его в файл `script/engine/meta/<taskId>-checklist.md`.  
   - По завершении работы задачи вызвать сценарий закрытия (например `SCN-CloseTask`), который:
     - переводит задачу в статус `Completed`
     - при наличии зависимой задачи (из `contextDependencies.requiredTasks` или `customFields.sourceTaskId`) автоматически активирует её через `SCN-ActivateTask`

**Важные требования к выводу:**  
- Не включай описание рассуждений или служебные заметки.  
- Выведи только Markdown-документ, в папку `script/engine/meta`, с чеклистом и разделом рекомендаций по CloseTask.  
- Строго следуй структуре и названиям разделов.  
- Используй Scan-Plan-Refine и QTU-стандарты для формулировок.