@invoke-scenario-engine.php при вызове без параметров - выдает общую информаци как это делает @main-work.ps1 . мейн
сечас , без задачи в маркере, выводит общую информацию, теперь эта роль уходит к @invoke-scenario-engine.php , а
@main-work.ps1 вернется в нормальное русло, тоесть опять верну @all-fix.ps1 на место мейн скрипта и только добавлю в
него вызов @invoke-scenario-engine.php 
