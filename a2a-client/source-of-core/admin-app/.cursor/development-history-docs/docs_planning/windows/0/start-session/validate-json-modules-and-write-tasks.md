Здравствуйте. Пожалуйста, действуйте как автономный AI-исполнитель для системы задач AI на основе методологии
Scan-Plan-Refine.

Ваша **основная цель** — делать правиль но модули, переделывать концептуальные модули в рабочий формат.
сначало запускаем @MergeModuleVersions.php с именем модуля или без имен, а потом запускаем по имени модуля
@ValidateModuleJsonCommand.php .
может не работать и еще я хочу сделать некоторые новведения в ядре, чтоб валидатор имел больше данных о сканируемом
обьекте. можем создать задачу, когда отсканируешь все что нужно.

Следуйте этим шагам:

1. **Сканируйте и понимайте основные механизмы:** Систематически исследуйте ключевые директории и файлы системы задач
   AI, чтобы построить основополагающее понимание. **Сосредоточьтесь не только на структуре, но и на том, *как*
   компоненты взаимодействуют.**
    * `script/engine/`:
        * `task_definitions/`: Изучите несколько образцов JSON задач (например, общую задачу, задачу, связанную со
          сбором данных).
        * `task_types/`: Посмотрите на `*.task_type_data.json` и соответствующий `*.template.json`.
        * `scenarios/`: **Анализируйте ключевые сценарии, такие как `SCN-ActivateTask.scenario.json`,
          `SCN-IndexCollectTaskData.scenario.json` (или его эквивалент для полного сбора данных) и
          `SCN-CloseTask.scenario.json`. Поймите их шаги, переменные контекста и вызываемые действия. *Обратите внимание
          на то, как переменные `context` передаются и изменяются через шаги, и как `nextStepId` (
          или `onSuccess`/`onError`) направляет поток.* **
        * `markers/`: Обратите внимание на соглашение о наименовании и структуру файлов маркеров прогресса. *Поймите,
          что файлы маркеров являются основным механизмом сохранения состояния для экземпляров сценариев.*
    * `script/docs/standards/`: Определите ключевые операционные стандарты, особенно:
        * `task-manager/task-definition-standard.json`
        * `ai-agent/operational-principles.json`
        * `ai-agent/question-to-user-standard.json`
        * `scenario-engine/scenario-standard.json`
        * **`ai-task-system-operation-standard.md` (если доступен, или аналогичные документы, описывающие работу
          системы).**
        * *Также просмотрите `script/docs/processes/` на наличие задокументированных операционных рабочих процессов (
          например, связанных с жизненным циклом задач, обработкой данных или разрешением ошибок), которые дополняют
          стандарты.*
    * Основные скрипты:
        * `main.ps1`: Поймите его роль как общего дашборда/исполнителя модулей.
        * `main-work.ps1` (и `script/main-work-dispatch.ps1`, если это активный скрипт): **Глубоко проанализируйте его
          режимы (`RunScenario`, `ShowTask`, `ResetScenario`), как он формирует аргументы для и вызывает
          `invoke-scenario-engine.php`, и как он обрабатывает результаты (статус, обновления контекста, взаимодействие с
          `system.state.json`). *Особенно обратите внимание на вспомогательные функции, используемые для подготовки
          аргументов для PHP (например, `New-PhpScenarioArgs` или аналогичные) и как `Invoke-ScenarioEngine` (или прямые
          вызовы PHP) выполняются и как их JSON-результаты обрабатываются. Поймите, как он
          использует `Get-ScenarioProgress` и `Save-SystemState`.* **
        * `main-index.ps1`: Поймите его возможности запроса и формат вывода. *Обратите внимание на то, как он формирует
          аргументы для `script/engine/index/index-processor.php` и какие форматы вывода он поддерживает. Поймите
          назначение его различных режимов (например, `query-keywords`, `get-metadata`).*
    * `script/engine/state/system.state.json`: Поймите его роль в хранении `activeTask`.
    * `script/includes/`: Проверьте наличие ключевых вспомогательных скриптов, таких как `logging.ps1`,
      `PhpInterop.ps1`.
    * ***Контроль и поток данных:** Составьте карту типичного потока управления и данных между скриптами
      PowerShell (`main-work.ps1`, `main-index.ps1`) и
      PHP-движком (`invoke-scenario-engine.php`, `index-processor.php`). Поймите, как аргументы, контекст и результаты
      передаются туда и обратно (например, аргументы командной строки, JSON-строки, закодированный в Base64 контекст).*

2. **Планируйте выполнение задач:** На основе сканирования сформулируйте детальный план того, как вы будете **автономно
   управлять типичным жизненным циклом задачи**:
    * **Активация задачи:** Как определить идентификатор задачи (например, из списка, по запросу пользователя или как
      зависимость) и **выполнить команду для запуска `SCN-ActivateTask` через `main-work.ps1`, предоставив необходимые
      входные данные и проверив обновление `system.state.json`. *Убедитесь, что `taskId`, предоставленный
      для `SCN-ActivateTask`, действителен и существует в `task_definitions/`.* **
    * **Сбор данных (если применимо):** Как определить `taskQuery` (например, из `taskDefinition.json` -> `linkedFiles`,
      `keywords` или логика, специфичная для типа задачи),
      ***правильно его сформировать в соответствии с требованиями ввода `main-index.ps1`или `SCN-IndexCollectTaskData` (
      например, JSON-данные, если `main-index.ps1` вызывается напрямую шагом, или специфические переменные контекста,
      если используется `SCN-IndexCollectTaskData`)***, **выполнить команду для `SCN-IndexCollectTaskData` (или
      эквивалентного сценария полного сбора) через `main-work.ps1`, и понять, где хранятся/сливаются `taskIndexResults`.
      *Проверьте, что `projectRoot` правильно передан, если сбор данных включает доступ к файловой системе или запросы
      индекса, охватывающие проект.* **
    * **Прогресс сценария:** Как интерпретировать статус `HaltedForInput` из `main-work.ps1`, извлечь
      `questionToDisplay` и **сформулировать следующую команду `main-work.ps1` с `-UserInputValue`, чтобы продолжить
      сценарий. *Поймите, что `-UserInputValue` имеет решающее значение для шагов `Question` и что AI должен
      предоставить именно тот ввод, который ожидается логикой сценария.* **
    * **Выполнение действий:** Поймите, что шаги `Action` в сценариях будут вызывать скрипты PowerShell или PHP, и их
      успех/неудача влияет на поток сценария.
    * **Использование маркеров:** Как проверить статус маркеров через `main-work.ps1` (если существует прямой режим "
      показать маркер") или путем вывода поведения `RunScenario` с и без `-ResetProgress`. **Поймите, что маркеры в
      основном управляются `invoke-scenario-engine.php`. *Признайте, когда может быть уместно
      использовать `-ResetProgress` с `main-work.ps1 -Mode RunScenario` (например, чтобы перезапустить неудавшийся
      сценарий с самого начала или если сценарий предназначен для идемпотентности и всегда должен выполняться заново,
      как некоторые сценарии сбора данных). Основание этого решения должно быть на дизайне сценария и требованиях
      задачи, а не на произвольных повторениях.* **
    * **Закрытие задачи:** Как **выполнить команду для `SCN-CloseTask` через `main-work.ps1` для завершенной задачи.**
    * **Обработка ошибок:** Как интерпретировать статусы и сообщения об ошибках из `main-work.ps1` /
      `invoke-scenario-engine.php` и решать, какие шаги предпринять дальше (например, повторить, спросить пользователя,
      записать ошибку). *Консультируйтесь с `script/docs/standards/ai-agent/iterative-debugging-standard.json` или
      аналогичными, если возникают постоянные ошибки. Записывайте ошибки, с которыми вы столкнулись, и решения, принятые
      для повторов или эскалаций.*
    * **Использование инструментов:** Четко укажите, какой скрипт (`main-work.ps1`, `main-index.ps1`) и режим вы будете
      использовать для каждой части жизненного цикла. *При планировании команд явно указывайте полную структуру
      команды `pwsh -File ./main-work.ps1 ...` или `pwsh -File ./main-index.ps1 ...` со всеми необходимыми параметрами и
      их предполагаемыми значениями. Соблюдайте правило `no-cat-in-pwsh` для любых сгенерированных команд PowerShell.*
    * ***Соблюдение рабочего процесса системы:** Убедитесь, что ваши запланированные действия соответствуют общим
      рабочим процессам системы, описанным в `script-workflow` (или `ai-task-system-operation-standard.md`) и любым
      соответствующим документам в `script/docs/processes/`.*
    * ***Операции на основе фактов:** Все решения и действия должны основываться на информации, которую можно проверить
      из состояния системы, файлов, выводов и задокументированных стандартов/процессов (
      см. `fact-based-operations-rule`).*

3. **Уточнение и выполнение:**
    * Опишите, как вы будете уточнять свое понимание на основе фактических результатов выполнения. *Это включает в себя
      проверку фактических результатов ваших команд (например, проверка создания/изменения файлов, обновленного статуса
      маркеров, изменений в `system.state.json`).*
    * **Укажите свою готовность выбрать образец задачи (или спросить пользователя о ней) и попытаться провести ее через
      жизненный цикл, используя инструменты и команды, которые вы запланировали.**
    * **Предложите простую первую задачу для управления, например, активацию существующей задачи или запуск сценария
      сбора данных для известной задачи.**
    * ***Проактивная проверка:** Прежде чем выполнять шаг, который изменяет состояние или файлы, если вы не уверены,
      рассмотрите процесс "сухого запуска" или используйте команду только для чтения, чтобы подтвердить предварительные
      условия.*
    * ***Запись намерений и действий:** Для сложных многошаговых операций кратко запишите свои намерения перед
      выполнением серии команд и подведите итог результату, что поможет в отслеживании.*

Представьте свои выводы из сканирования и свой детализированный план выполнения. Явно укажите файлы, директории и *
*конкретные команды**, которые вы намерены использовать (используя `mdc:` для файлов/директорий).

--- 

Если вам нужна дополнительная помощь или конкретные детали, не стесняйтесь спрашивать!
