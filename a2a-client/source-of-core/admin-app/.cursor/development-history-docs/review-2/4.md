Okay, I acknowledge your feedback on the questions. I note there might be a difference in interpretation regarding the stability of the `session:` prefix and the contents of the `commands/` directory in `login-form/v1` compared to the documentation I reviewed, but I will proceed with scanning the next files from the list as you instructed.

I have already reviewed many of the items that appeared to be specific file paths or directories from your initial list. Based on that list and the files I've reviewed, the next distinct file/directory concept that hasn't been explicitly covered as a standalone read (beyond mentions in other documents) appears to be:

*   `@docs-mirrors` (This is likely a directory containing mirrored documentation).

I will attempt to list the contents of a potential `docs-mirrors/` directory to understand what it contains.

Ran tool
Okay, I have listed the contents of the `docs-mirrors/` directory.

It contains subdirectories mirroring parts of the project structure (`routes/`, `app/`, `tests/`, `resources/`, `script/`) and two files: `README.md` and `ve-file.md`. This confirms that `docs-mirrors/` is indeed used for mirroring parts of the codebase, likely for documentation or analysis purposes.

I will now read the `README.md` file in the `docs-mirrors/` directory to understand its purpose.

Read file: docs-mirrors/README.md

Okay, I have read the `README.md` in the `docs-mirrors/` directory. It explains that this folder contains markdown files that serve as documentation mirrors for specific source code files in the project. The structure within `docs-mirrors/` mimics the source code file locations.

Based on your initial list and the files reviewed so far, I will now examine `install-modules/aiCore/js/component-map.json`. This file was referenced in `docs/start-session/true-module-fix.md` as the source of truth for supported UI component types.

Read file: install-modules/aiCore/js/component-map.json

Okay, I have reviewed `install-modules/aiCore/js/component-map.json`. This file provides a mapping of component names (like `button`, `form`, `inputtext`) to their categories (`Component`, `Container`, `VModel`, `ComplexContainer`, `Customs`). This mapping is essential for understanding the valid UI component types supported by the system.

Here are 5 yes/no questions based on the contents of this `component-map.json` file:

1.  According to this `component-map.json` file, are components like `button`, `icon`, and `image` categorized under "Component"?
2.  Does this map categorize `form`, `row`, and `column` components under the "VModel" category?
3.  Based on this file, is the `checkbox` component listed under the "ComplexContainer" category?
4.  According to this `component-map.json`, is the `datatable` component classified as a "ComplexContainer"?
5.  Does this map list `panel` as belonging to the "Component" category?

I will now examine a file within the `script/engine/scenarios/` directory to understand the practical structure of a scenario definition, as this directory was listed in the initial directory listing of `script/engine/` but no specific scenario file has been read yet. I will read `script/engine/scenarios/SCN-ActivateTask.scenario.json` since "ActivateTask" was mentioned as a key scenario in `true-module-fix.md` for managing task lifecycle.

Read file: script/engine/scenarios/SCN-ActivateTask.scenario.json

Okay, I have reviewed the `script/engine/scenarios/SCN-ActivateTask.scenario.json` file. This scenario is used to set a specific task as the active task and update its status to "InProgress".

Here are 5 yes/no questions based on the contents of this scenario definition:

1.  Does the scenario require a `taskId` parameter to be provided for it to run?
2.  Does the final step of the scenario explicitly set the overall scenario status to "ERROR"?
3.  Is the very first step of this scenario designed to execute a PHP script?
4.  Does the scenario's definition indicate that it will provide a variable named `activeTask` in the context upon successful completion?
5.  Does the scenario definition include a step with the unique identifier "START"?

I will now continue by examining `script/engine/task_definitions/JsonModuleDevelopment-00018.json`, as it appears to be a specific example of a task definition file, relevant to the task management discussed in `true-module-fix.md`.

Read file: script/engine/task_definitions/JsonModuleDevelopment-00018.json

Okay, I have reviewed the `script/engine/task_definitions/JsonModuleDevelopment-00018.json` file. This file defines a task to refactor the `question-to-user` module to comply with project standards, using the `true-module-fix.md` methodology. It outlines the goal, steps, acceptance criteria, and relevant linked files and exemplar modules.

Here are 5 yes/no questions based on the contents of `script/engine/task_definitions/JsonModuleDevelopment-00018.json`:

1.  Does the task definition explicitly list "Refactor the 'question-to-user' module" as its primary goal?
2.  Does the task require comparing the module against exemplar modules like `primary-form/v1` and `login-form/v1`?
3.  Is passing the `php artisan validate:module-json` command listed as an acceptance criterion for this task?
4.  Does the task definition specify that the target module is located at `implement-modules/question-to-user/v5/`?
5.  Does the task explicitly state that all UI components used in the refactored module must *not* be listed in `component-map.json`?
