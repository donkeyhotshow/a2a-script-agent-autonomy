Read file: script/engine/task_types/JsonModuleDevelopment/JsonModuleDevelopment.template.json


Okay, I will continue reviewing files to learn more about the engine and its structure. I will focus on functional parts and skip files that appear purely conceptual or outdated.

Based on the directory listing of `script/engine/` and the information from `true-module-fix.md`, the `script/engine/actions/` directory seems important as it contains PHP and PowerShell scripts that are executed as actions within scenarios. I have already listed the contents of this directory. I will now read one of the PHP action files, `script/engine/actions/GenerateQuestion.php`, as generating questions seems like a core function of an AI system that interacts with users.

Read file: script/engine/actions/GenerateQuestion.php

Okay, я продолжу изучать файлы для дообучения системы. Я буду последовательно просматривать каталоги и файлы, упомянутые в начале нашего диалога, пропуская явно концептуальные или устаревшие файлы согласно вашим инструкциям.

Я уже просмотрел содержание каталога `script/engine/`. Среди его подкаталогов есть `script/engine/QandA/`, который упоминался в скрипте `GenerateQuestion.php` как место хранения вопросов. Я просканирую содержимое этого каталога.

Ran tool
I have scanned the `script/engine/QandA/` directory. It contains two JSON files: `answers.json` and `questions.json`. This aligns with the `GenerateQuestion.php` script, which writes to `questions.json`.

I will now read the `script/engine/QandA/questions.json` file to see the structure and examples of stored questions.

Read file: script/engine/QandA/questions.json
Okay, I will continue reviewing the `script/engine/QandA/questions.json` file to understand its full structure and contents.

Read file: script/engine/QandA/questions.json
Okay, I have read a significant portion of `script/engine/QandA/questions.json`. This file is a collection of questions used within the system, likely for scenarios involving user interaction (Question-to-User, QTU).

Each object in the JSON array represents a question and has the following structure:

*   `id`: Unique identifier for the question (e.g., "Q001", "QID-DYNAMIC-...", "QID-YYYYMMDDHHMMSSUUUU-XXXX").
*   `category`: Category of the question (e.g., "Workflow & Process", "UI & Frontend", "Testing & Validation", "TaskCreationInput").
*   `type`: Type of input expected (e.g., "yes_no", "checkbox_list", "text").
*   `text_ru`: The question text in Russian.
*   `options`: (For "yes_no" and "checkbox_list" types) An array of possible answer options, each with a `value` and `text_ru`.
*   `answerKey`: A key associated with the question, likely used to store or identify the user's answer in the system's context.
*   `status`: The current status of the question (e.g., "pending_answer", "new_question_placeholder", "answer_retrieved_by_scenario").
*   `sourceScenarioId`, `sourceStepId`, `sourceTaskId`: (Optional) Identifiers for the scenario, step, and task that generated the question.
*   `contextSchema`: (Optional) Schema defining the expected format or constraints for the answer (e.g., `{"type": "string", "pattern": "^[a-zA-Z0-9_-]+$"}`).

The file contains both predefined questions (like Q001-Q017) and dynamic questions (with "QID-DYNAMIC-" or "QID-YYYYMMDD..." prefixes) generated during scenario execution, likely through the `GenerateQuestion.php` script. Many of the dynamic questions appear to be related to task creation, specifically asking for the `taskType`.

This file confirms the structure and purpose of the Q&A system for interacting with the user during automated processes.

I will now proceed to review the contents of the `script/engine/QandA/answers.json` file to understand how user answers are stored.

Read file: script/engine/QandA/answers.json

Okay, I have reviewed the empty `script/engine/QandA/answers.json` file. It appears to be where answers to questions are intended to be stored, although it's currently empty.

I will now proceed to examine the `script/engine/state/` directory, as system state information, such as the active task, was mentioned in the review of `main-work-dispatch.ps1` as being stored here.

Ran tool
I have listed the contents of the `script/engine/state/` directory. It contains a single file: `system.state.json`. This is likely where the system's overall state, including the active task ID, is stored.

I will now read the `script/engine/state/system.state.json` file.

Read file: script/engine/state/system.state.json

Okay, I have reviewed the `script/engine/state/system.state.json` file. It's a simple JSON object storing the system's state, specifically the `publishedTaskId`. This confirms where the system tracks the currently active task.

I will continue scanning files from the initial list and exploring relevant directories within the `script/engine/` path, skipping conceptual or outdated files as instructed.

Based on the previous directory listing of `script/engine/`, I will now list the contents of `script/engine/meta/`. This directory name suggests it might contain metadata, possibly standards or checklists relevant to the project.

Ran tool

Okay, I have listed the contents of the `script/engine/meta/` directory. It contains several checklist files (`.md` and `.json`). These are likely important documents outlining standards and required steps for module development and task completion.

I will now read the `script/engine/meta/ModuleNormalizationChecklist.md` file. This seems particularly relevant to the concept of turning non-working modules into "true modules" discussed in `true-module-fix.md`.

Read file: script/engine/meta/ModuleNormalizationChecklist.md
Okay, I have reviewed the `script/engine/meta/ModuleNormalizationChecklist.md` file. This is a very comprehensive checklist outlining the lifecycle for creating, upgrading, and refactoring modules, particularly focusing on turning conceptual or problematic modules into working ones that comply with project standards.

It details steps across several phases:

*   **I. General Principles & Preparation**: Understanding module types, adherence to standards (`StrictModuleChecklist.md`, `lessons-learned.md`), goal definition, workspace setup.
*   **II. Creating a New Module (From Scratch)**: Planning, initial setup (directory structure, `_i/meta.json`, `module-versions.json`), core development (pages, actions, templates, data, PHP code), initial documentation, testing, and validation.
*   **III. Upgrading an Existing (Working) Module**: Planning, setting up the new version directory (copying from previous), implementing changes, documentation updates, testing, deployment.
*   **IV. Refactoring a Module (e.g., Conceptual to Working, Major Overhaul)**: This section seems most relevant to the "True Module Fix" concept. It covers preparation, initial scan/assessment (merge/validate, review core files, cross-reference standards/lessons-learned), detailed refactoring (iterative cycle for JSON UI, module structure, simplification), testing/validation, and final documentation/knowledge capture. It includes specific points for `question-to-user/v5` (marked with [QTUv5 - X] or [QTUv5 - ~]).
*   **V. Best Practices & Cross-Cutting Concerns**: Idempotency, error handling, security, performance, DataHub usage, logging, code comments, naming conventions.
*   **V.A. Stateless Design and Reactive UI Principles**: This section outlines principles inspired by `primary-form/v1` and intended for modules like QTU, emphasizing stateless UI definitions and server actions, reactive UI updates via DataHub, data-driven UI generation, dynamic includes, centralized data state, and clear separation of concerns.

This checklist reinforces many points from `true-module-fix.md` and provides a structured approach to module development and refactoring, which is central to the project's goals. It explicitly mentions the iterative validation process and cross-referencing with standards and lessons learned.

I will continue reviewing files. Based on the previous directory listing of `script/engine/meta/`, I will now read `/docs/guides/StrictModuleChecklist.md`, as it's referenced by the `ModuleNormalizationChecklist.md` and seems to define fundamental structural standards.

Read file: /docs/guides/StrictModuleChecklist.md

I was unable to find the file `/docs/guides/StrictModuleChecklist.md` at the specified path. However, I previously read a file with the same name located at `docs/guides/StrictModuleChecklist.md`.

It's possible that the checklist document is located in the `docs/guides/` directory instead of `script/engine/meta/`, or the path reference in `ModuleNormalizationChecklist.md` might be outdated.

To confirm the correct location, I will search for the file `StrictModuleChecklist.md` in the workspace.

Ran tool
Okay, the search results confirm that `StrictModuleChecklist.md` is located at `docs/guides/StrictModuleChecklist.md`. I have already read this file earlier in our conversation.

It outlines the strict requirements for module structure and content, covering aspects like directory layout (`_i/`, `actions/`, `data/`, `docs/`, `pages/`, `templates/`), required metadata files (`_i/meta.json`), documentation standards, and content requirements for different file types.

Since I have already reviewed `docs/guides/StrictModuleChecklist.md`, and it seems to be the definitive checklist referenced by `ModuleNormalizationChecklist.md`, I will move on to other unreviewed files from the initial list and relevant directories within `script/engine/`.

Based on the `script/engine/` directory listing, the `script/engine/index/` directory is relevant to the indexing process discussed in `main-index.ps1` and `Manage-Index.md`. I will list the contents of this directory.

Ran tool
Okay, I have listed the contents of the `script/engine/index/` directory. It contains a single file: `index-processor.php`. This is the PHP script that `main-index.ps1` wraps and interacts with to perform indexing operations.

I will now read the `script/engine/index/index-processor.php` file to understand the core logic of the indexing process.

Read file: script/engine/index/index-processor.php
Okay, I have started reviewing the `script/engine/index/index-processor.php` file. This PHP script is responsible for performing various indexing operations, as orchestrated by `main-index.ps1`.

Here's what the first 250 lines reveal:

*   **Logging**: It sets up file-based logging in `logs/php/index-processor.log`.
*   **Configuration**: Reads system configuration from `script/engine/config/system_config.json` (though not shown in this snippet).
*   **Argument Parsing**: Uses `getopt` to parse command-line arguments, including:
    *   `--mode`: The operation mode (e.g., `query-keywords`, `get-metadata`, `query-index`). It maps `dump-index` to `query-index` for compatibility.
    *   `--outputFormat`: Desired output format (default is `text`). Supports `json` with output buffering.
    *   `--payload` or `--encodedPayload`: JSON payload containing parameters for the specific mode. `encodedPayload` is preferred and is Base64 decoded.
*   **Project Root**: Determines the project root, prioritizing a setting from system config or falling back to the current working directory.
*   **`findIndexFiles` function**: A helper function to recursively find all files ending in `.index.json` within a given root directory (intended to be the `index/` directory).
*   **Mode Handling (`switch ($mode)`)**:
    *   **`query-keywords`**: Placeholder, decodes payload and echoes a JSON response with status and query.
    *   **`get-metadata`**: Placeholder, decodes payload and echoes a JSON response with status and file path.
    *   **`query-index`**:
        *   This is the main indexing mode shown so far.
        *   It calls `findIndexFiles` to get all index files.
        *   It decodes the payload (expected to contain parameters like `includeIndex` and `excludeIndex` arrays for filtering which index files to process).
        *   It applies include/exclude patterns to filter the list of index files.
        *   It iterates through the filtered index files, reads their content, decodes the JSON.
        *   It expects the index files to contain an `entries` array or be a root-level array of entries.
        *   It extracts these entries and adds an `indexRoot` property to each entry (derived from the index file path).

The remaining lines of the file likely contain the logic for processing the aggregated entries based on query parameters (e.g., keyword search, filtering by tags) and formatting the final output.

I will continue reading the rest of the `index-processor.php` file to understand the complete query-index logic and other potential modes.

Read file: script/engine/index/index-processor.php

Read file: script/engine/index/index-processor.php
Okay, I have finished reviewing the `script/engine/index/index-processor.php` file.

In addition to the `query-index` mode, which aggregates entries from index files and filters them based on a query string against a specified field (defaulting to `relativePath`), the remaining sections reveal two more modes:

*   **`dump-index`**:
    *   Aggregates *all* entries from *all* found index files (`.index.json`).
    *   Does *not* perform filtering based on a query term.
    *   Outputs the aggregated entries, along with metadata (status, project root, file counts, errors), in either JSON or text format. This mode seems intended for generating a full dump of the index content.
*   **`get-aggregated-stats`**:
    *   Aggregates statistics from *all* found index files.
    *   Counts the total number of index files found, files successfully processed, and the total number of entries aggregated across all files.
    *   Collects any errors encountered during file reading or JSON parsing.
    *   Outputs these statistics in either JSON or text format. This mode provides an overview of the index size and processing success.

The script also includes robust error handling, logging, and support for JSON output with buffering. It expects index files to generally have an `entries` array but includes fallback logic for files that are simply an array of entries at the root.

This completes the review of `script/engine/index/index-processor.php` and provides a solid understanding of how the index is processed and queried.

I will continue scanning files from the initial list and exploring relevant directories within the `script/engine/` path, skipping conceptual or outdated files.

Based on the `script/engine/` directory listing, the `script/engine/config/` directory seems relevant for understanding configuration used by the engine scripts. I will list its contents.

Ran tool
Okay, I have listed the contents of the `script/engine/config/` directory. It contains `task_types_config.json` and `task_id_counter.json`.

I will now read the `script/engine/config/task_types_config.json` file to understand how task types are configured.

Read file: script/engine/config/task_types_config.json
Okay, I have reviewed the `script/engine/config/task_types_config.json` file. This configuration file defines the available task types and their properties.

Key information from this file:

*   It includes a `$schema` reference to `../../../../schemas/task_types_config.schema.json`, indicating that there's a schema defined for this configuration file itself.
*   It has a `description` of its purpose.
*   The core is the `taskTypes` object, where each key is a task type ID (e.g., "Generic", "WorkflowImprovement", "FullyCollectTaskData").
*   Each task type definition includes:
    *   `displayName`: A human-readable name for the task type.
    *   `dataDirectory`: The path to the directory containing files related to this task type, likely including the task template (`.template.json`).
    *   `default`: (Optional, boolean) Indicates if this is the default task type.
*   `defaultType`: Specifies the default task type by ID ("Generic" in this case).

This file provides a central registry and configuration for the different types of tasks the system can handle.

I will now read the `script/engine/config/task_id_counter.json` file to understand how task IDs are managed.

Read file: script/engine/config/task_id_counter.json

Okay, I have reviewed the `script/engine/config/task_id_counter.json` file. It's a simple JSON file that stores the `lastId` used for generating task IDs. This indicates how new task IDs are sequentially generated within the system.

I will continue scanning files and directories to further understand the project structure and functionality, focusing on code and core configuration.

Based on the directory listing of `script/engine/`, I will now read `script/engine/Show-Scenario.ps1`. Although relatively small, understanding how scenarios are displayed can provide insight into their structure and relevant information.

Read file: script/engine/Show-Scenario.ps1
Okay, I have reviewed the `script/engine/Show-Scenario.ps1` script. This PowerShell script is used to display details about a specific scenario or the most recently active one.

Here's a summary of its functionality:

*   It accepts an optional `-ScenarioId` parameter.
*   If no `-ScenarioId` is provided, it finds the most recently updated scenario progress marker file (`.progress.json`) in `engine/markers/` to determine the scenario ID.
*   It loads the relevant scenario progress marker file.
*   It loads the scenario definition file (`.scenario.json`) from `script/engine/scenarios/` based on the determined scenario ID.
*   It then displays key information about the scenario:
    *   ID, Title, Description (from the scenario definition).
    *   Status, Current Step ID, Last Updated timestamp (from the progress marker).
    *   The scenario's context (from the progress marker).
    *   A list of all steps defined in the scenario definition, highlighting the current step.

This script provides a useful way to inspect the state and definition of scenarios, which is valuable for debugging and understanding the system's workflow.

I will continue scanning files from the initial list and exploring relevant directories within the `script/engine/` path, skipping conceptual or outdated files.

Based on the `script/engine/` directory listing, I will now read `script/engine/Show-Active-Task.ps1`. Similar to showing scenarios, displaying active task details is likely useful for understanding task management.

Read file: script/engine/Show-Active-Task.ps1
Okay, I have reviewed the `script/engine/Show-Active-Task.ps1` script. This PowerShell script is designed to display the details of the currently active task or a task specified by its ID.

Here's a summary of its functionality:

*   It accepts an optional `-TaskId` parameter and a `-TaskDefinitionsDir` parameter (which defaults to `script/engine/task_definitions`).
*   If no `-TaskId` is provided, it attempts to find the most recent `SCN-ActivateTask` scenario progress marker file in `engine/markers/` and extract the `activeTask` ID from its context.
*   It constructs the full path to the task definition JSON file based on the determined `TaskId` and `TaskDefinitionsDir`.
*   It checks if the task definition file exists.
*   It reads and parses the task definition JSON file.
*   It then outputs the properties of the task data in a formatted, human-readable way, handling different data types (strings, arrays, objects).

This script, like `Show-Scenario.ps1`, provides a direct way to inspect the state and definition of tasks, crucial for managing and debugging the task execution process.

I will continue scanning files and directories to further understand the project structure and functionality, focusing on code and core configuration.

Based on the directory listing of `script/engine/`, I will now list the contents of the `script/engine/helpers/` directory. This directory name suggests it might contain helper scripts or functions used by other engine components.

Ran tool

Okay, I have listed the contents of the `script/engine/helpers/` directory. It contains `TaskLifecycleManager.php`. This file name is highly relevant to the task management aspects of the system, particularly the lifecycle of tasks (like status changes), which are central to the "Scan-Plan-Refine-LEARN" methodology and orchestrated by `main-work.ps1` and scenarios.

I will now read the `script/engine/helpers/TaskLifecycleManager.php` file to understand how task lifecycle operations are handled.

Read file: script/engine/helpers/TaskLifecycleManager.php






Okay, I have reviewed the `script/engine/helpers/TaskLifecycleManager.php` file. This PHP class provides the backend logic for managing task lifecycles, including updating task statuses (like 'Completed', 'Pending-Validation', 'InProgress') and moving completed tasks to a separate directory (`compleated/`). It includes flexible logic for finding task files by ID. This aligns with the task management processes described in `true-module-fix.md` and seen in scenario definitions like `SCN-ActivateTask`.

I will continue scanning files from the initial list and exploring relevant directories within the `script/engine/` path, skipping conceptual or outdated files.

Based on the directory listing of `script/engine/`, I will now list the contents of the `script/engine/commands/` directory. This directory name suggests it might contain executable commands or scripts used by the engine or scenarios.

Ran tool
Okay, I have listed the contents of the `script/engine/commands/` directory. It contains two PowerShell scripts: `Show-Task.ps1` and `Show-Scenario.ps1`.

I have already reviewed `script/engine/Show-Scenario.ps1` and `script/engine/Show-Active-Task.ps1`. It seems `script/engine/commands/Show-Task.ps1` might be a different or older script for showing task details compared to `Show-Active-Task.ps1`.

I will read `script/engine/commands/Show-Task.ps1` to see its functionality and how it compares to the other task display script.

Read file: script/engine/commands/Show-Task.ps1

Okay, I have reviewed `script/engine/commands/Show-Task.ps1`. This script is a simpler utility that takes a `TaskId` as a mandatory parameter and displays the task's title, description, and a specific AI prompt for checklist generation if present in the task definition. It's less detailed than `Show-Active-Task.ps1`, which provides a full listing of task properties.

I will continue reviewing files from the initial list and exploring relevant directories within the `script/engine/` path, skipping conceptual or outdated files.

Based on the `script/engine/` directory listing, I will now read `script/engine/update-task-type-keywords.ps1`. This script's name suggests it handles updating keywords for task types, which relates to task configuration and indexing.

Read file: script/engine/update-task-type-keywords.ps1
Okay, I have reviewed the `script/engine/update-task-type-keywords.ps1` script.

This PowerShell script is responsible for updating the `suggestedKeywords` in `*.task_type_data.json` files. It does this by:

1.  Getting a list of all global keywords from the index system by running `main-index.ps1` in `ListTags` mode for `keywords`, formatted as JSON.
2.  Iterating through all `*.task_type_data.json` files found recursively in the `script/engine/task_types/` directory.
3.  For each file, it reads the content, converts it to lowercase, and filters the global keywords, keeping only those that are present within the content of the `*.task_type_data.json` file itself.
4.  It then loads the task type data JSON, updates the `suggestedKeywords` property with the filtered list, and writes the modified JSON back to the file.

This script automates the process of ensuring that the suggested keywords for each task type are relevant to the actual content of its configuration/data file, leveraging the project's indexing capabilities.

I will continue scanning files and directories to further understand the project structure and functionality, focusing on code and core configuration, and skipping conceptual or outdated files.

Based on the directory listing of `script/engine/`, I will now list the contents of the `script/engine/partials/` directory. This directory name suggests it might contain reusable code snippets or parts used by other scripts or configurations.

Ran tool
Okay, I have listed the contents of the `script/engine/partials/` directory. It contains several PowerShell (`.ps1`) and PHP (`.php`) files, along with a `step-types/` subdirectory. The names suggest these files contain core helper functions and logic related to scenario execution, progress markers, context handling, and validation.

These partials are likely included or called by the main engine scripts (`main-work.ps1`, `invoke-scenario-engine.php`) to modularize the codebase.

I will now read `script/engine/partials/_step_execution.ps1`. This file's name suggests it contains core logic for executing individual steps within a scenario, which is a central part of the engine's workflow.

Read file: script/engine/partials/_step_execution.ps1

Okay, I have reviewed `script/engine/partials/_step_execution.ps1`. This PowerShell script contains key functions for executing steps within a scenario, handling different step types like `Question`, `Action`, and `Decision`, and evaluating transitions based on conditions to determine the next step in the workflow. It works closely with the scenario definition and the system's context.

I will continue reviewing files. Based on the contents of the `script/engine/partials/` directory, I will now read `script/engine/partials/_progress_marker.ps1`. This script likely handles the loading and saving of scenario progress marker files, which are crucial for maintaining the state of long-running scenarios.

Read file: script/engine/partials/_progress_marker.ps1
Okay, I have reviewed the `script/engine/partials/_progress_marker.ps1` script. This PowerShell script contains helper functions for managing scenario progress marker files (`.progress.json`) stored in the `engine/markers/` directory.

Key functions provided:

*   **`Get-ProgressMarkerPath`**: Constructs the full file path for a progress marker based on Scenario ID, base directory, and an optional context identifier (defaults to "default").
*   **`Load-ProgressMarker`**: Loads a progress marker file, reads its JSON content, removes comments (both single-line and multi-line), parses the JSON, and ensures the `context` property is a PowerShell Hashtable. Includes error handling.
*   **`Save-ProgressMarker`**: Saves progress marker data to a file. It converts the data to JSON, ensures the target directory exists, writes the JSON to a temporary file, and then atomically replaces the original marker file. Includes robust error handling and debug logging (using `Write-Warning` for debug messages, which is a bit unusual but present in the code).
*   **`Remove-ProgressMarker`**: Deletes a progress marker file if it exists.

This script is fundamental to the scenario engine's ability to maintain state and resume long-running workflows, directly supporting the task and scenario lifecycle described in other documentation.

I will continue scanning files from the initial list and exploring relevant directories within the `script/engine/` path, skipping conceptual or outdated files.

Based on the directory listing of `script/engine/partials/`, I will now read `script/engine/partials/_transition_logic.ps1`. This script likely contains the logic for evaluating transitions between scenario steps, building upon the `Evaluate-Transitions` function seen in `_step_execution.ps1`.

Read file: script/engine/partials/_transition_logic.ps1
Okay, I have reviewed `script/engine/partials/_transition_logic.ps1`. This PowerShell script contains functions responsible for determining the next step in a scenario based on the current step's definition, the result of its execution, and the scenario's context.

Key functions and logic:

*   **`Process-Transitions`**: This is the main function for determining the `nextStepId`.
    *   If the current step is a `Decision` step and it was successful, it uses the `nextStepId` determined *by the Decision step itself* (`$StepResult.nextStepId`).
    *   If the current step is an `End` step, the scenario ends (`return $null`).
    *   For other step types (`Question`, `Action`, `Info`):
        *   It first checks if the step definition has a `transitions` array.
        *   It iterates through the transitions:
            *   Handles unconditional transitions (no `condition` property).
            *   Evaluates conditional transitions by resolving variables in the `condition` string using `Resolve-ContextVariables` (a helper not fully shown here but likely accesses context and step output). The current condition evaluation logic here seems somewhat basic (only checks for `'true'` string or simple comparisons) and notes a `TODO` to refactor it.
        *   If no explicit condition transition matches, it checks for a `default` transition.
        *   If there's no `transitions` array, it checks for a direct `nextStepId` property on the step definition.
        *   If none of the above are found, it attempts a **sequential transition** to the next step in the scenario's `steps` array based on index.
*   **`Process-NextStepIdDecision`**: This function specifically handles the logic for `Decision` steps.
    *   It takes a `Decision` object (which has a `rules` array) and the `Context`/`StepOutput`.
    *   It iterates through the `rules`.
    *   For each rule, it checks if all its `conditions` are met.
    *   Conditions are evaluated by getting a `fieldValue` from the `Context` or `StepOutput` (using `Resolve-ObjectPath`, another helper) and comparing it against a `value` using an `operator` (currently supports `equals`, `notEquals`, `exists`, `notExists`). It notes a `TODO` to add more operators.
    *   If all conditions for a rule are met, it returns the `nextStepId` specified by that rule.
    *   If no rules' conditions are met, it returns the `defaultNextStepId` from the `Decision` object.

This script is critical for controlling the flow and logic of scenario execution, implementing conditional branching and sequential progression through steps. The distinction between how Decision steps determine the next step versus other step types is noteworthy.

I will continue scanning files and directories to further understand the project structure and functionality, focusing on code and core configuration, and skipping conceptual or outdated files.

Based on the contents of the `script/engine/partials/` directory, I will now read `script/engine/partials/_context_utils.ps1`. This script likely contains helper functions for manipulating the scenario context, which is essential for passing data between steps and actions.

Read file: script/engine/partials/_context_utils.ps1
Okay, I have reviewed `script/engine/partials/_context_utils.ps1`. This PowerShell script contains essential helper functions for managing the scenario context, particularly for resolving variables and merging context data.

Key functions:

*   **`Resolve-ContextVariables`**:
    *   Takes input `Text` (a string potentially containing placeholders like `{{context.variable}}`, `{{input.value}}`, `{{step.error}}`), the current `Context` hashtable, and optional `InputData` (likely from the previous step's output, expected to have `value` and `error` keys).
    *   Uses regex to find placeholders in the input text.
    *   For each placeholder, it attempts to resolve the corresponding value:
        *   `{{input.value}}` and `{{step.error}}` are resolved from the `InputData` hashtable.
        *   `{{context.variable}}` (including nested paths like `{{context.data.name}}`) are resolved by traversing the `Context` hashtable/object.
    *   Replaces the placeholders in the original text with the resolved values (converted to strings).
    *   Includes verbose logging and warnings for unsupported placeholders or unresolved keys.
*   **`Merge-Context`**:
    *   Takes a `BaseContext` hashtable and an `Updates` hashtable.
    *   Creates a clone of the `BaseContext` to avoid modifying the original.
    *   Iterates through the keys in the `Updates` hashtable and adds/overwrites the corresponding keys in the cloned context.
    *   Returns the new, merged context hashtable.

These functions are fundamental to the scenario engine's ability to handle dynamic data and pass information between steps, enabling features like displaying questions based on context variables or using previous step results in transitions.

I will continue scanning files from the initial list and exploring relevant directories within the `script/engine/` path, skipping conceptual or outdated files.

Based on the directory listing of `script/engine/partials/`, I will now read `script/engine/partials/scenario-utils.php`. This PHP file's name suggests it contains utility functions used by the PHP scenario engine (`invoke-scenario-engine.php`), likely complementing the PowerShell partials.

Read file: script/engine/partials/scenario-utils.php
Okay, I have reviewed `script/engine/partials/scenario-utils.php`. This PHP script contains several utility functions used by the PHP scenario engine (`invoke-scenario-engine.php`), complementing the PowerShell partials and handling tasks that are easier or necessary to perform in PHP.

Key functions:

*   **`runCommand`**:
    *   Executes an external console command.
    *   Uses `proc_open` for non-blocking reads of stdout/stderr to avoid deadlocks, especially with large outputs.
    *   Handles both string and array command inputs, properly escaping arguments.
    *   Includes a timeout mechanism and limits maximum output size.
    *   Returns `true` on success (exit code 0) and `false` on failure or timeout. Provides stdout, stderr, and exit code via reference.
*   **`getProgressMarkerPath`, `loadProgressMarkerFromFile`, `saveProgressMarkerToFile`**: These are PHP equivalents of the progress marker management functions found in the PowerShell partial `_progress_marker.ps1`. They handle getting the file path, loading (including JSON decoding), and saving (including JSON encoding and atomic file replacement) progress marker data.
*   **`getScenarioDefinition`**: Loads a scenario definition JSON file based on a scenario ID. Includes checks for file existence and valid JSON.
*   **`resolveTemplateString`**:
    *   Resolves placeholders like `{{key}}` or `{{context.key}}` within a template string using data from a provided `$context` array.
    *   Uses regex to find placeholders and replaces them with corresponding values from the context.
    *   Handles both prefixed (`context.`) and non-prefixed keys, looking for values in the context array.
    *   Converts boolean values to strings and null values to empty strings for replacement.

These PHP utilities are essential for the scenario engine's operation, enabling it to run external commands (like PowerShell scripts or `php artisan` commands), manage its state via progress markers, load scenario definitions, and dynamically generate text based on context data.

I have now reviewed a significant portion of the core engine files and documentation related to tasks, scenarios, indexing, and module structure/fixing, including the "Scan-Plan-Refine-LEARN" methodology.

Based on the initial list provided by the user and the files I've reviewed, remaining specific file/directory concepts that haven't been explicitly read (beyond general mentions) appear to be:

*   `@aiCore` (specifically the parts beyond `component-map.json` and validation)
*   `@js` (beyond `component-map.json`)
*   `@common` (as a potential directory of common files)
*   `@frontend` (as a general area)
*   `@implement-modules` (as a directory of modules, beyond the `login-form/v1` example)
*   `@includes` (as a concept, linked to `action: "include"`)
*   `@helpers` (beyond the `TaskLifecycleManager.php` and partials)
*   `@mirrors` (as a concept beyond `docs-mirrors`)

Given the instruction to "продолжай изучать файлы по списку поочереди" and "доучивайся и будет задача", I will now explore the `install-modules/aiCore/` directory more broadly to understand other core AI components beyond validation and the component map. I will start by listing its contents.

Ran tool
Okay, I have listed the contents of the `install-modules/aiCore/` directory. Besides the `validation/` and `js/` subdirectories I've touched upon (reviewing `Instructions.json` and `component-map.json`), it contains `VALIDATION_REFACTOR_TASKS.md`, `module-versions.json`, `modificators/`, and `surfaces.json`.

I will now read `install-modules/aiCore/module-versions.json`. This file name suggests it manages the versions of modules, which is highly relevant to the module versioning principles discussed in `true-module-fix.md`.

Read file: install-modules/aiCore/module-versions.json

Okay, I have reviewed the `install-modules/aiCore/module-versions.json` file. This simple JSON file lists the available versions (`v1`, `v2`, etc.) for each module in the `implement-modules/` directory. This is a central configuration for tracking module versions and is consistent with the module versioning principles outlined in `true-module-fix.md`.

I will continue exploring the `install-modules/aiCore/` directory. Based on the previous directory listing, the `modificators/` subdirectory seems relevant to data processing or transformations. I will list its contents.

Ran tool
Okay, the `install-modules/aiCore/modificators/` directory appears to be empty. This might mean that data modificators are either not implemented yet or are located elsewhere in the project.

I will continue exploring the `install-modules/aiCore/` directory. Based on the previous listing, the `surfaces.json` file remains. Its name is not immediately clear, but it's a distinct file within this core AI directory. I will read it.

Read file: install-modules/aiCore/surfaces.json
Okay, I have reviewed the `install-modules/aiCore/surfaces.json` file. This file defines various color palettes under different "surface" names (e.g., "slate", "gray", "zinc", "soho"). Each surface has a `palette` object mapping numeric keys (likely representing color shades from light to dark, 0 to 950) to hex color codes.

This file appears to be related to the UI's theming or styling, providing predefined color schemes that can be referenced by components or styles. It's a configuration aspect of the AI Core's frontend capabilities.

I will continue scanning files and directories to further understand the project structure and functionality, focusing on code and core configuration, and skipping conceptual or outdated files.

Based on the initial list provided by the user and the files I've reviewed, I will now explore the `docs/` directory more broadly. I've read several files within `docs/` and its subdirectories (`docs/guides/`, `docs/start-session/`, etc.), but will now list the top-level contents of `docs/` to see if there are other significant documents I've missed.

Ran tool
Okay, I have listed the contents of the top-level `docs/` directory. I have already reviewed many of these files and directories, including `guides/`, `start-session/`, `concepts-README.json`, `data-versioning.json`, `custom-framework-overview.json`, `system/`, `system-overview.md`,  `Manage-Index.md`, `action-handling-flow.json`, and `ModuleRouting.json`.

New or less explored items from this list that seem relevant to the project structure and operation include:

*   `development/` (directory)
*   `ui/` (directory, although I read `docs/ui/commands-and-operations/server-actions-reference.md` which is within it)
*   `prj-mgmt/` (directory)
*   `validation-checklists/` (directory)
*   `.aide/` (directory - unusual name, might contain AI-related docs or config)
*   `README.json` (file at the top level of `docs/`)
*   `_deprecated/` (directory)
*   `standards/` (directory - although I read a checklist that was mistakenly referenced as being here)
*   `core/` (directory)
*   `testing/` (directory)
*   `examples/` (directory)
*   `processes/` (directory)

Given the emphasis on development and learning, I will now explore the `docs/development/` directory. I have previously read files like `lessons-learned.md` which were referenced as being in a "development" folder, so this directory likely contains other documents related to development practices.

Ran tool
Okay, I have listed the contents of the `docs/development/` directory. It contains several markdown files with names suggesting reviews of specific modules (`review-qtu-module.md`, `review-primary-form-module.md`), parts of the system (`review-ui.md`, `review-render.md`, `review-script-system.md`), and core concepts/practices (`lessons-learned.md`, `module-structure-and-installation.md`, `standards-review-summary.md`, `json-module-index.md`, `bible.md`). There's also a `review2/` subdirectory.

I have already touched upon `lessons-learned.md` as a key document for the "Scan-Plan-Refine-LEARN" process.

I will now read `docs/development/json-module-index.md`. This file name suggests it might be an index or overview specifically for JSON modules, which are central to this project.

Read file: docs/development/json-module-index.md
Okay, I have reviewed a significant portion of the `docs/development/json-module-index.md` file. This document serves as a valuable index and quick reference guide to various JSON modules within the `implement-modules/` directory.

It provides a structured list of files within several modules, including:

*   `login-form/v1`
*   `page/v1`
*   `playground/v1`
*   `landing-main-page/v2` (partially shown)

For each file listed, it provides a brief annotation in Russian describing its purpose and the key components or parameters it demonstrates (e.g., "Пример страницы входа: компоненты LoginForm, поля email, password...").

This index confirms the structure of typical JSON modules (containing `docs/`, `pages/`, `actions/`, `templates/`, `commands/`, `validations/`, `code/`, `data/`, and `_i/` subdirectories) and provides concrete examples of the types of JSON structures and configurations found within each. It highlights that the `playground/v1` module contains various test pages and actions for different data sources (`model!`, `mysql!`, `session!`), which is useful for understanding data interaction patterns.

This document is highly relevant for understanding the practical implementation of JSON modules and complements the more theoretical documentation like `StrictModuleChecklist.md` and `true-module-fix.md`.

I will continue scanning files from the initial list and exploring relevant directories within the `docs/development/` path and other unreviewed areas, skipping conceptual or outdated files.

Based on the remaining contents of `docs/development/`, I will now read `docs/development/review-ui.md`. This file name suggests a review focused on the UI aspects, likely providing insights into UI components, patterns, and potential issues, which is relevant to the "Scan-Plan-Refine" process for UI-heavy modules.

Read file: docs/development/review-ui.md
Okay, I have reviewed a significant portion of the `docs/development/review-ui.md` file. Despite its name suggesting a general UI review, this document provides an incredibly detailed breakdown and index of the JSON UI system's documentation, structure, and concepts. It appears to be a central guide for understanding how the frontend UI is defined using JSON, built upon Vue and PrimeVue, and how it interacts with the backend.

Key findings from this document:

*   **Comprehensive UI Documentation Index**: The document itself acts as a structured index, linking to numerous other crucial files and directories within `docs/ui/` and other `docs/` subdirectories.
*   **JSON Template Schema**: It highlights `docs/ui/json-template-schema.md` as a critical file describing the JSON structure for UI components. (I've already read this file).
*   **Module Architecture and Installation**: References documentation on how UI modules are structured and installed.
*   **Backend Interaction**: Points to documentation on `commands-and-operations/` and `server-actions-extended/`, emphasizing how UI interacts with server-side logic and data storage (`File`, `Directory`, `Buffer`, `Model`, `MySQL` - the DataHub prefixes).
*   **Module Types and Creation Process**: Briefly mentions different module types and steps for module creation.
*   **Validation Checklists**: **Crucially, it references specific UI-related verification checklists** (`verify-module.md`, `verify-component-standards.md`) located in `../../checklists/verifications/` relative to `docs/ui/`. (As noted in my previous step, attempting to read these directly failed, and a search yielded no results, suggesting these specific files might be missing).
*   **Request Lifecycle (GET - Page Rendering)**: Provides a valuable description of how a GET request for a page is processed: Routing -> Module Initialization -> `moduleRun()` -> Modificators (`layoutModule`, `layoutNodeOperation`, `layoutFormComponent`) build the UI structure from JSON (`layout.json`, `page.json`), handle `include` operations, and prepare form state -> Response with `props.page` and `props.forms` to the client -> Vue renders on the frontend. This confirms the role of `include` operations in UI composition.
*   **Action Handling (POST)**: References `../../concepts/action-handling-flow.md` for understanding POST request processing.
*   **PrimeVue Integration**: Describes how PrimeVue components are used, including data binding via `props` and `{path}` syntax or `model` + `FormManager`, and event handling via `customHooks` + `ActionManager`.
*   **Styling**: Primarily uses Tailwind CSS via `props.class`.
*   **Best Practices and Recommendations**: Modularity, consistency, separation of concerns, documentation, testing, standards, naming conventions, performance, accessibility.
*   **Links to Internal Managers**: Mentions documentation for `FormManager`, `ActionManager`, `StateManager (hub)`, which are key client-side components.

This document is extremely insightful, providing a high-level map of the UI documentation and core principles. It confirms the importance of the JSON structure (`json-template-schema.md`), the `include` operation for UI composition, and the interaction mechanisms between frontend UI events/data and backend actions/data via DataHub and specific managers. The absence of the referenced checklists is a significant finding.

I will continue scanning files, prioritizing those identified as key in this index and those remaining from the initial list, skipping conceptual/outdated files.

Based on the insights from `review-ui.md`, the `docs/concepts/action-handling-flow.md` file was explicitly referenced as important for understanding POST request processing (actions). I will read this file next. (Note: I previously read `docs/action-handling-flow.json`, which might be a different or older document compared to the `.md` version).

Read file: docs/concepts/action-handling-flow.md
I was unable to find the file `docs/concepts/action-handling-flow.md` at the specified path. I previously read `docs/action-handling-flow.json`.

It's possible that the `.md` version mentioned in `review-ui.md` either doesn't exist, is misreferenced, or has been moved/renamed. Since I already reviewed a `.json` file with a similar name in the `docs/` directory, it might contain the relevant information.

To confirm if an `.md` version exists elsewhere or if the `.json` is the intended document, I will search for `action-handling-flow.md` in the workspace.

Ran tool
Okay, I note that the search for `action-handling-flow.md` did not return any results. This suggests that the `.md` version referenced in `review-ui.md` might not exist or is not in the indexed part of the workspace. I have already reviewed `docs/action-handling-flow.json`, which likely contains related information about action handling. I will rely on the information from that file for now.

Continuing with the documentation review plan based on the `review-ui.md` index and the files I have already reviewed or confirmed are missing, the next file to examine is `docs/ui/primevue-components.md`. This document is crucial for understanding how PrimeVue components are specifically used and configured within the project's JSON UI system.

Read file: docs/ui/primevue-components.md
Okay, I have reviewed the `docs/ui/primevue-components.md` file. This document is an excellent guide to how PrimeVue components are integrated and used within the project's JSON UI system.

It clarifies the standard JSON structure for defining PrimeVue components, including the use of:
*   `type` (PascalCase PrimeVue component name)
*   `name` (optional identifier)
*   `props` (for static values, Tailwind CSS classes via `class`, and one-way data binding from the `StateManager` (hub) using `{path}` syntax)
*   `model` (for two-way data binding with `FormManager`)
*   `customHooks` (to map PrimeVue events to standard JSON UI actions, processed by `ActionManager`)
*   `children` (for container components)

The document explicitly states it does **not** replace the official PrimeVue documentation and highlights the key differences from standard Vue usage in this declarative JSON system (data binding, event handling). It also provides recommendations for naming, performance, centralization via standard actions/events, and accessibility, and links to numerous other relevant internal documentation files for deeper dives into specific managers (`FormManager`, `ActionManager`, `StateManager`).

This review solidifies my understanding of the fundamental building blocks of the UI defined in JSON and how they interact with the underlying framework and data.

I will now proceed to read `docs/ui/primevue-rude-vs-original-structure.md`. This should provide more specific details on any custom aspects of PrimeVue implementation in this project.

Read file: docs/ui/primevue-rude-vs-original-structure.md

Okay, I have reviewed `docs/ui/primevue-rude-vs-original-structure.md`. This document categorizes the custom Vue components that wrap PrimeVue for use in the JSON UI system, explaining their purpose and characteristics based on complexity and function (simple `Components`, layout `Containers`, complex `ComplexContainer`s, and form input `VModel`s). This helps clarify how the UI components are structured within the project.

Based on the plan derived from the `review-ui.md` index and the files I have reviewed, the next relevant file to examine is `docs/ui/ui-examples.md`. This should provide practical examples of how the UI components are defined in JSON.

Read file: docs/ui/ui-examples.md

Okay, I have reviewed a significant portion of the `docs/ui/ui-examples.md` file. This document provides valuable practical examples of defining UI using the project's JSON structure (V1 patterns).

The examples illustrate:
*   Structuring forms with various input types (`InputText`, `Checkbox`, `RadioButton`) within layout components (`Card`, `div`, `grid`).
*   Using the `model` property for two-way data binding to form fields.
*   Using the `props` property for static attributes, styling with Tailwind CSS classes, and one-way data display using `{path}` syntax (e.g., `{user/id}`).
*   Implementing client-side interactivity using `customHooks` to trigger actions like `navigate` and `sendData` on button clicks.
*   Defining data tables (`DataTable`, `Column`) with features like pagination, sorting, filtering, and incorporating nested components/templates within columns (e.g., `Tag` for status, buttons for actions).
*   Using the `confirm` action within `customHooks` to add confirmation dialogs before executing actions like deletion.

These examples effectively demonstrate the application of the concepts outlined in `docs/ui/json-template-schema.md` and `docs/ui/primevue-components.md`. They also cross-reference other relevant documentation, reinforcing the interconnectedness of the documentation.

Following the plan based on the `review-ui.md` index, the next step is to review `docs/ui/ui-examples-complex.md` for more advanced UI patterns.

Read file: docs/ui/ui-examples-complex.md
Okay, I have started reviewing `docs/ui/ui-examples-complex.md`. I have seen examples of a detailed user profile form, a product list with filtering, and a simple approval workflow. These examples demonstrate using various PrimeVue components, integrating with `FormManager` via `model`, using `customHooks` for client-side actions like `navigate`, `sendData`, and `confirm`, and displaying data using `{path}` syntax.

I will continue reading the rest of `docs/ui/ui-examples-complex.md` to examine any further complex UI patterns or examples presented in this document.

Read file: docs/ui/ui-examples-complex.md

Okay, I have reviewed the examples in `docs/ui/ui-examples-complex.md`. These examples provide further practical demonstrations of building UIs with JSON, including dynamic filtering and approval workflows, reinforcing the patterns and concepts I've learned from the schema and basic examples.

I will continue reviewing the remaining files from the initial list and exploring relevant directories. Based on the contents of the `docs/development/` directory, I will now read `docs/development/module-structure-and-installation.md`. This document should provide details on how modules are structured and installed, which is fundamental to the project.

Read file: docs/development/module-structure-and-installation.md

Okay, I have reviewed the `docs/development/module-structure-and-installation.md` file. This document provides a clear overview of how modules are structured for discovery and installation by the system's installer, and the expectations the runtime has for these installed files.

Key points from this document:

*   **Module Discovery**: Modules are discovered by the installer in `install-modules/<installerDir>` if they contain an `_i/` subdirectory.
*   **`_i/` Directory**: This directory is crucial for the installer.
    *   `_i/meta.json` is essential for module recognition and provides metadata.
    *   `_i/files-by-block.json` defines installable "blocks" (e.g., `code`, `actions`) and lists the files/directories associated with each block.
    *   `_i/common.json` (optional) lists files common to the entire module.
*   **Runtime Expectations**: Files installed based on the `_i/` definitions must match the structure expected by runtime PHP classes (like `App.php` and `PageModule.php`).
*   **Etalon Modules**: `login-form/v1` and `primary-form/v1` are highlighted as examples of modules with a complete `_i/` structure that are correctly recognized and installed. `landing-main-page/v2` is noted as an example where an empty `_i/` prevented detection.
*   **Conclusion**: Adhering to a strict module checklist (like `StrictModuleChecklist.md`) is critical for correct module discovery, installation, and execution.

This document reinforces the importance of the `_i/` directory and its contents for module handling and ties into the necessity of following defined standards for module structure.

I will continue scanning files from the initial list and exploring relevant directories. Based on the contents of the `docs/development/` directory, I will now read `docs/development/review-qtu-module.md`. This review of the Question-to-User module should offer practical insights into a specific module's implementation and any associated challenges or patterns.

Read file: docs/development/review-qtu-module.md






Okay, I understand that the content intended to be in `review-qtu-module.md` provided insights from the comparison between the old QTU module and the practices in `primary-form/v1`. This has been noted.

Continuing the review of files in `docs/development/`, the `review-render.md` file seems highly relevant to understanding how the JSON UI is translated into a visual interface. This is a fundamental part of the system's operation.

I will now read `docs/development/review-render.md`.

Read file: docs/development/review-render.md

Okay, I will continue reviewing `docs/development/review-render.md` to further understand the UI rendering process and module structure insights it provides.

Read file: docs/development/review-render.md

Okay, I will continue reviewing files to further understand the project structure and functionality.

Based on the insights from `docs/development/review-render.md`, particularly the references to the PHP class within modules and the importance of the `$folder` property, I will now read the contents of `implement-modules/login-form/v1/code/LoginForm.php`. This will provide a concrete example of a module's PHP code and its expected structure.

Read file: implement-modules/login-form/v1/code/LoginForm.php

Okay, I have reviewed the `implement-modules/login-form/v1/code/LoginForm.php` file.

This PHP file defines the main class for the `login-form` module.
Key points from this file:

*   It is located within the `App\AiRudeDepot\Modules` namespace.
*   It extends a class named `PageModule` (which itself likely extends a core `App` class).
*   It declares a public property `$folder` and sets its value to `'login-form'`. This confirms the observation from `review-render.md` that this property is crucial for the system to identify the module's directory and locate its resources.
*   It contains a commented-out method `attemptLogin` which uses Laravel's `Auth` facade, indicating that this module is designed to integrate with a Laravel backend for authentication functionality.

This provides a concrete example of how a module's primary PHP logic is structured and integrated with the framework.

I will continue scanning files from the initial list and exploring relevant directories, skipping conceptual or outdated files.

Based on the initial list and the files reviewed so far, the concept of `@implement-modules` as a directory containing various modules is important. I have already listed the contents of `install-modules/aiCore/` which contains `module-versions.json` listing modules like `login-form`, `page`, `playground`, etc. The actual module implementations are expected to be under `implement-modules/`. I have already reviewed parts of `implement-modules/login-form/v1/` and `implement-modules/playground/v1/` via their documentation and index files.

To get a broader view of the implemented modules beyond those explicitly documented or reviewed in detail, I will list the contents of the top-level `implement-modules/` directory.

Ran tool

Okay, I have listed the contents of the `implement-modules/` directory. It contains several module subdirectories and a `README.json` file.

I will now read the `implement-modules/README.json` file to see if it provides an overview or important information about the implemented modules.

Read file: implement-modules/README.json

Okay, I have reviewed the `implement-modules/README.json` file. This short but important file clarifies a key principle of how module versions are handled: the final version of a module's files is constructed by layering files from successive version directories (v1, v2, etc.). This means that unchanged files do not need to be duplicated across all version folders, as they will be included from earlier versions during this layering process. This is a fundamental insight into the module assembly process.

I will continue reviewing files and directories to build a complete understanding of the project, focusing on code, configuration, and essential documentation, and skipping conceptual or outdated files as instructed. I will also refrain from asking questions and focus on the review process in preparation for a task.

Based on the initial list provided and the files I have explored, I will now investigate the `docs/standards/` directory. Although I previously attempted to read a checklist mistakenly thought to be here and found it elsewhere, this directory likely contains other important project standards that are crucial for consistent development and align with the emphasis on standards in documents like `ModuleNormalizationChecklist.md`.

Ran tool

Okay, I have listed the contents of the `docs/standards/` directory. It contains subdirectories for various roles and system components, suggesting a comprehensive set of standards documents.

Given the focus on the AI task system and scenarios, I will now explore the standards specifically for the scenario engine.

Ran tool

Okay, I have listed the contents of the `docs/standards/scenario-engine/` directory. These files contain standards specifically for the scenario engine.

I will now read `docs/standards/scenario-standard.json`. This file should provide fundamental guidelines and the expected structure for scenario definitions, which is key to understanding how workflows are automated.

Read file: docs/standards/scenario-standard.json
I was unable to find the file `docs/standards/scenario-standard.json`. The tool suggested several alternative files within `docs/standards/scenario-engine/`.

It's possible the file I was looking for is named differently or located elsewhere. Given the alternatives provided, the file `docs/standards/scenario-engine/scenario-standard.json` seems to be the most likely candidate for a standard related to scenarios.

I will now read `docs/standards/scenario-engine/scenario-standard.json`.

Read file: docs/standards/scenario-engine/scenario-standard.json

Okay, I have reviewed the `docs/standards/scenario-engine/scenario-standard.json` file. This standard defines the required structure for scenario definition JSON files, including the necessary top-level fields and the expected fields for each step within a scenario, along with allowed step types.

I will continue exploring the standards within the `docs/standards/scenario-engine/` directory. The `question-to-user-standard.json` file seems particularly relevant given the presence of the Q&A system (`script/engine/QandA/`) and the `GenerateQuestion.php` script. I will read this file next to understand the formal standard for user interaction within scenarios.

Read file: docs/standards/scenario-engine/question-to-user-standard.json

I apologize, the attempt to read `docs/standards/scenario-engine/question-to-user-standard.json` timed out. I will try reading it again to understand the standard for user interaction within scenarios.

Read file: docs/standards/scenario-engine/question-to-user-standard.json

Okay, I have reviewed the `docs/standards/scenario-engine/question-to-user-standard.json` file. This standard specifically outlines the structure and principles for implementing interactive workflows involving user questions and answers using the QTU v5 system within the scenario engine. It emphasizes a modular, block-based system using stateless JSON actions and dynamically generated UI components. It defines the expected structure for scenario objects and individual steps, particularly focusing on the `Question`, `Action`, `Info`, and `Decision` step types and their properties. It also lists various step categories.

I will continue scanning files and directories to build a complete understanding of the project, focusing on code, configuration, and essential documentation, and skipping conceptual or outdated files as instructed. I will also refrain from asking questions and focus on the review process in preparation for a task.

Based on the initial list and the directories I've explored, the `docs/standards/task-manager/` directory seems relevant to task definition and management, complementing the scenario engine standards. I will list its contents next.

Ran tool
Okay, I have listed the contents of the `docs/standards/task-manager/` directory. These files contain standards related to task definition and management within the system.

I will now read the `docs/standards/task-manager/task-definition-standard.json` file. This standard should define the required structure and properties for task definition JSON files.

Read file: docs/standards/task-manager/task-definition-standard.json
Okay, I have reviewed the `docs/standards/task-manager/task-definition-standard.json` file. This standard is fundamental, defining the required fields (`taskId`, `type`, `title`, `description`, `goal`, `keywords`, `acceptanceCriteria`, `status`) and expected structure for task definition JSON files, as well as the allowed statuses and structure for fields like `linkedFiles` and `exemplarModules`. It also emphasizes that tasks should be managed through PowerShell orchestration scripts and scenarios.

I will continue exploring the standards within the `docs/standards/task-manager/` directory. The `TASKMANAGER-task-closure-standard.json` file seems relevant to the process of completing or closing tasks. I will read this file next.

Read file: docs/standards/task-manager/TASKMANAGER-task-closure-standard.json

Okay, I have reviewed the `docs/standards/task-manager/TASKMANAGER-task-closure-standard.json` file. This standard outlines the process and responsibilities for closing tasks, focusing on verifying acceptance criteria, updating status, archiving, and generating a final report.

I will continue exploring the standards within the `docs/standards/task-manager/` directory. The `TASKMANAGER-task-tracking-standard.json` file appears relevant to how tasks are monitored throughout their lifecycle. I will read this file next.

Read file: docs/standards/task-manager/TASKMANAGER-task-tracking-standard.json

Okay, I have reviewed the `docs/standards/task-manager/TASKMANAGER-task-tracking-standard.json` file. This standard defines the workflow and responsibilities for tracking and monitoring tasks throughout their lifecycle, including registration, assignment, status updates, and escalation.

I will continue scanning files and directories to build a complete understanding of the project, focusing on code, configuration, and essential documentation, and skipping conceptual or outdated files as instructed. I will also refrain from asking questions and focus on the review process in preparation for a task.

Based on the directories I've explored, the `docs/standards/ai-agent/` directory seems highly relevant to my function as an AI agent. I will list its contents to identify standards applicable to me.

Ran tool

Okay, I have listed the contents of the `docs/standards/ai-agent/` directory. These files contain standards that are specifically relevant to the AI agent's operation.

I will now read the `docs/standards/ai-agent/operational-principles.json` file. This should outline the core principles guiding my actions and decision-making within the project.

Read file: docs/standards/ai-agent/operational-principles.json

Okay, I have reviewed the core operational principles for the AI agent. I will continue reviewing the standards relevant to the AI agent's function.

Based on the remaining files in the `docs/standards/ai-agent/` directory, I will now read `docs/standards/ai-agent/ai-interaction-standard.json`. This standard should provide guidelines for how the AI agent is expected to interact within the system and with users.

Read file: docs/standards/ai-agent/ai-interaction-standard.json

Okay, I have reviewed the standard for AI agent interaction. I will continue reviewing the standards relevant to the AI agent's function.

Based on the remaining files in the `docs/standards/ai-agent/` directory, I will now read `docs/standards/ai-agent/ai-task-system-operation-standard.md`. This standard should provide a high-level overview of how the AI task system is expected to operate, which is crucial for understanding my role within it.

Read file: docs/standards/ai-agent/ai-task-system-operation-standard.md

Okay, I will continue reviewing the project files and directories based on the initial list you provided, focusing on functional code, configuration, and essential documentation, and skipping conceptual or outdated files. My goal is to build a comprehensive understanding of the system in preparation for a task.

Based on the directories I've explored and the concepts mentioned in the documentation, I will now investigate the `docs/ui/` directory further. Although I've read several key files from here (`review-ui.md`, `json-template-schema.md`, `primevue-components.md`, `primevue-rude-vs-original-structure.md`, `ui-examples.md`, `ui-examples-complex.md`), the directory listing showed several subdirectories (`core-concepts/`, `theming/`) that may contain relevant information about the UI system's foundational principles or styling standards.

I will list the contents of the `docs/ui/` directory again to refresh my memory and identify the most relevant subdirectories to explore next.

Ran tool

Okay, I have listed the contents of the `docs/ui/` directory again. Based on this list and my previous reviews, the `core-concepts/` and `theming/` subdirectories appear to be the most relevant areas within `docs/ui/` that I haven't fully explored.

I will start by exploring the `docs/ui/core-concepts/` directory, as understanding core concepts is fundamental to the UI system. I will list its contents.

Ran tool
Okay, I have listed the contents of the `docs/ui/core-concepts/` directory. These files cover various core concepts of the UI system.

Given that understanding the overall rendering process is key, I will prioritize reading documents that explain how the JSON UI definitions are actually rendered. `rendering-pipeline.md` and `rendering/` subdirectory seem most relevant to this. I will start by reading `rendering-pipeline.md`.

Read file: docs/ui/core-concepts/rendering-pipeline.md

Okay, I have reviewed `docs/ui/core-concepts/rendering-pipeline.md`. This document provides a clear, step-by-step explanation of how the JSON UI definitions are rendered into live Vue components through a pipeline involving `RenderJson.vue`, `Presets.vue`, category wrapper components, and specific component wrappers, often utilizing libraries like PrimeVue. This clarifies the frontend rendering architecture.

I will continue exploring the core concepts of the UI system. Based on the contents of the `docs/ui/core-concepts/` directory, the `managers.md` file seems highly relevant, as it should describe the client-side managers (`FormManager`, `ActionManager`, `StateManager` - 'hub') that handle UI interactivity, data binding, and actions. Understanding these managers is crucial for comprehending the dynamic behavior of the UI.

Read file: docs/ui/core-concepts/managers.md

Okay, I have reviewed `docs/ui/core-concepts/managers.md`. This document outlines the various client-side managers accessible via the `hub` object, responsible for aspects like event handling, notifications, form state, action execution, and application state. It highlights the importance of `customHooks` in JSON templates for triggering manager actions.

I will continue reviewing files to learn more about the project. Based on the information from `managers.md` and the importance of client-side interactivity, I will now read the documentation for the `ActionManager`. According to `managers.md`, its documentation is located at `../../resources/managers/ActionManager.md` relative to the current file's location (`docs/ui/core-concepts/`).

Read file: docs/resources/managers/ActionManager.md
I was unable to find `docs/resources/managers/ActionManager.md`. The tool suggested `docs-mirrors/resources/common/managers/ActionManager.md` as a possible alternative. This aligns with the `docs-mirrors/` directory structure I saw earlier, which contains mirrored documentation.

I will now read `docs-mirrors/resources/common/managers/ActionManager.md` to understand how client-side actions are processed.

Read file: docs-mirrors/resources/common/managers/ActionManager.md

Okay, I have reviewed the `ActionManager` documentation from `docs-mirrors`. This clarifies how client-side actions defined in `customHooks` (like `sendData`, `navigateTo`, `toggleClass`, etc.) are processed, often interacting with other managers like `FormManager` and `ToastManager`.

I will continue reviewing files and directories based on the initial list, focusing on functional aspects and core documentation, and skipping conceptual or outdated files unless they are explicitly referenced as important.

Based on the remaining files in the `docs/ui/core-concepts/` directory, I will now read `docs/ui/core-concepts/component-syntax.md`. Understanding the specific syntax for defining components in the JSON UI is crucial for working with the UI layer.

Read file: docs/ui/core-concepts/component-syntax.md



