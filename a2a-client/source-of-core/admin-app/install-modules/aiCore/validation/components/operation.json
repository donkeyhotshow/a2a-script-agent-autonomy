{
    "componentName": "operation",
    "description": "Represents a special processing instruction (e.g., include, add).",
    "level": 0,
    "status": "active",
    "dependencies": [],
    "nestedValidation": {
        "structure": {
            "action": {
                "type": "string",
                "required": true,
                "description": "The type of operation (e.g., 'include', 'add').",
                "options": [
                    "include",
                    "add"
                ]
            },
            "source": {
                "type": "string",
                "required": true,
                "description": "The source path or identifier for the operation."
            },
            "$ref": "../common/root.json"
        }
    },
    "implementationNotes": [
        "Basic rule to validate required 'action' and 'source' keys.",
        "This is not a real UI component, but a processing directive."
    ],
    "documentationLink": "docs/guides/module/06-actions-logic.md"
}
