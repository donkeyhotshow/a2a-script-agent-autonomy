# ADR-005: RAG система на клиенте

## Дата
2026-02-25

## Авторы
- Команда разработки

## Статус
Принято

## Контекст

Система A2A Script Agent использует RAG (Retrieval-Augmented Generation) на стороне клиента для индексации и поиска по кодовой базе. Клиент хранит все индексы локально и выполняет поиск без обращения к серверу.

### Требования к RAG

- **Индексация кода:** Поддержка различных языков программирования
- **Hybrid Search:** Комбинация BM25 и semantic search
- **Chunking:** Интеллектуальное разбиение кода на чанки
- **Обновление индексов:** Инкрементальная индексация при изменениях

## Решение

### Архитектура RAG системы

```
a2a-client/packages/rag/
├── src/
│   ├── indexer.ts          # Индексация файлов
│   ├── searcher.ts         # Поиск по индексам
│   ├── chunk-manager.ts    # Управление чанками
│   ├── hybrid-search.ts    # Hybrid search (BM25 + semantic)
│   ├── semantic-search.ts  # Semantic поиск
│   ├── bm25.ts             # BM25 алгоритм
│   ├── reranker.ts         # Переранжирование результатов
│   ├── ast-chunker.ts     # AST-based чанкинг
│   ├── indexer.ts          # Главный индексер
│   └── ...
```

### Использование RAG на клиенте

```typescript
import { RAGIndexer } from '@a2a-client/rag';
import { HybridSearch } from '@a2a-client/rag';

// Инициализация индексера
const indexer = new RAGIndexer({
  rootPath: '/path/to/codebase',
  ignorePatterns: ['node_modules', '.git', 'dist'],
});

// Индексация кодовой базы
await indexer.index();

// Поиск
const searcher = new HybridSearch({
  bm25Weight: 0.3,
  semanticWeight: 0.7,
});

const results = await searcher.search('find auth middleware', { topK: 10 });
```

### Индексация кода

#### Поддерживаемые языки

| Язык | Расширения | Особенности |
|------|------------|-------------|
| TypeScript | .ts, .tsx | AST parsing, type extraction |
| JavaScript | .js, .jsx | AST parsing |
| Python | .py | AST parsing, docstrings |
| Java | .java | Class/interface parsing |
| C# | .cs | Class parsing |
| Go | .go | Function/type parsing |
| Rust | .rs | Module/crate parsing |

#### Индексация файлов

```typescript
interface FileIndexer {
  scan(rootPath: string, ignorePatterns: string[]): Promise<FileInfo[]>;
  index(files: FileInfo[]): Promise<void>;
  update(changedFiles: FileInfo[]): Promise<void>;
  remove(filePaths: string[]): Promise<void>;
}

interface FileInfo {
  path: string;
  content: string;
  language: string;
  size: number;
  modifiedAt: Date;
  hash: string;
}
```

### Hybrid Search (BM25 + Semantic)

```typescript
interface HybridSearchConfig {
  bm25Weight: number;      // Вес BM25 (по умолчанию 0.3)
  semanticWeight: number;   // Вес semantic (по умолчанию 0.7)
  topK: number;            // Количество результатов
  rerank: boolean;         // Применять переранжирование
}

class HybridSearch {
  async search(query: string, config: HybridSearchConfig): Promise<SearchResult[]> {
    // Параллельный поиск
    const [bm25Results, semanticResults] = await Promise.all([
      this.bm25.search(query),
      this.semantic.search(query),
    ]);

    // Нормализация и объединение результатов
    const normalizedBm25 = this.normalizeResults(bm25Results);
    const normalizedSemantic = this.normalizeResults(semanticResults);

    // Взвешенное объединение
    const combined = this.combineResults(
      normalizedBm25,
      normalizedSemantic,
      config.bm25Weight,
      config.semanticWeight
    );

    // Переранжирование
    if (config.rerank) {
      return this.reranker.rerank(query, combined, config.topK);
    }

    return combined.slice(0, config.topK);
  }
}
```

### Chunking стратегии

| Стратегия | Описание | Преимущества | Недостатки |
|-----------|----------|--------------|------------|
| `fixed` | Фиксированный размер | Простота | Может разрывать логические блоки |
| `by_language` | По грамматике языка | Сохраняет контекст | Сложность реализации |
| `by_function` | По функциям/классам | Логические единицы | Неравномерный размер |
| `recursive` | Иерархический | Адаптивность | Вычислительно сложнее |

```typescript
interface ChunkConfig {
  strategy: 'fixed' | 'by_language' | 'by_function' | 'recursive';
  chunkSize: number;        // Максимальный размер чанка
  overlap: number;          // Перекрытие между чанками
  minChunkSize: number;    // Минимальный размер
}

class ChunkManager {
  async chunk(file: FileInfo, config: ChunkConfig): Promise<Chunk[]> {
    switch (config.strategy) {
      case 'fixed':
        return this.fixedChunking(file, config);
      case 'by_language':
        return this.languageAwareChunking(file, config);
      case 'by_function':
        return this.functionChunking(file, config);
      case 'recursive':
        return this.recursiveChunking(file, config);
    }
  }
}
```

### Интеграция с Agent

```typescript
import { Agent } from '@a2a-client/agent';
import { HybridSearch } from '@a2a-client/rag';

const agent = new Agent({
  // Автоматический поиск по коду при обработке задач
  onTask: async (task) => {
    const searcher = new HybridSearch();
    const context = await searcher.search(task.input.query, { topK: 5 });
    return { context };
  },
});
```

### RAG Search Result

```typescript
interface RAGSearchResult {
  chunks: Chunk[];
  totalHits: number;
  queryTime: number;
  searchMethod: 'bm25' | 'semantic' | 'hybrid';
}
```

## Последствия

### Положительные

- **Локальная обработка:** Нет зависимости от внешних сервисов
- **Контекстуальность:** Сохранение связей между кодом
- **Гибкость:** Настраиваемые стратегии поиска
- **Инкрементальность:** Быстрое обновление индексов

### Отрицательные

- **Ресурсоёмкость:** Требует памяти для индексов
- **Сложность:** Множество компонентов для поддержки

### Ограничения

- Max file size: 1MB
- Max index size: 10GB
- Supported files: code, markdown, text

## Связанные ADR

- [ADR-001: Архитектура A2A Script Agent (Client)](./ADR-001.md)
- [ADR-003: State Machine (PhaseMachine)](./ADR-003.md)

## История изменений

| Дата | Версия | Описание |
|------|--------|----------|
| 2026-02-25 | 1.0 | Начальная версия ADR |
