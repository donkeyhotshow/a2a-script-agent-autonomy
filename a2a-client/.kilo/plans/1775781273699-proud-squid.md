# Plan: Distribute Tests to Their Own Packages

## Goal
Complete the reorganization of test files by moving remaining tests from the root `tests/` directory to appropriate package `tests/` subdirectories. This will improve code organization and make it easier to run package-specific tests.

## Current Status
Most tests have already been moved. Current distribution:
- **Tests in packages:** ~70 test files across various packages
- **Tests remaining in root:** 9 test files (integration, e2e, standalone)
- **Tests still in root/unit that need moving:** 1 test file

## Remaining Tasks

### Tests to Move
Move to `packages/vite-plugin/tests/`:
- `tests/unit/vite-config-routing.test.js`

### Tests to remain in root `tests/`
- `tests/integration/web-app.test.js` (integration across packages)
- `tests/integration/dialog-flow.test.mjs` (integration)
- `tests/integration/session-storage.test.js` (integration)
- `tests/integration/api-server-sessions.test.js` (integration)
- `tests/integration/api-server.test.js` (integration)
- `tests/single-scene.test.js` (standalone test)
- `tests/search.test.js` (standalone test)
- `tests/integration/hub-proxy-client-api.test.js` (already moved to sdk)

## Implementation Steps

### 1. Move remaining test file
Use `mv` command to move `tests/unit/vite-config-routing.test.js` to `packages/vite-plugin/tests/`

### 2. Update import paths
Update the relative import paths in the moved test file to reflect its new location.

### 3. Clean up empty directories
Remove `tests/unit/` and `tests/human-review/` directories if they become empty.

### 4. Verify tests run correctly
Run the test suite to ensure all moved tests work with updated paths.

## Validation
- Run `npm test` to ensure all tests pass
- Verify that test counts match expectations
- Check that CI/CD pipelines work correctly

## Risks and Considerations
- Ensure import path updates are correct
- Verify that vitest configurations include the new test locations
- Make sure integration tests that span packages remain in root</content>
<parameter name="filePath">.kilo/plans/1775781273699-proud-squid.md